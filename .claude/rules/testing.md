# Testing

⚠ **`MUST` = 必須、`SHOULD` = 原則、`MAY` = 任意。**

⚠ **どの検査を、どの順で回すかは [`verify`](../skills/verify/SKILL.md) が正本。**⚠ ここには書かない。
⚠ **ここが持つのは「何を優先して確かめるか」だけ。**

## 目的

⚠ **「実装したこと」ではなく、⚠ 「利用者に必要な挙動が壊れていないこと」**を確かめる。

- MUST: ⚠ **テストが通ったことを、⚠ 検証の完了と呼ばない**（正本は `CLAUDE.md` §1）。

## 優先順位

1. Domain の変換・正規化
2. 状態の遷移
3. ⚠ **欠損・失敗のとき**
4. 利用者の主な操作
5. 見た目の細部

## Domain

- MUST: ⚠ **Source → Domain の変換に、代表ケースを持つ。**
- MUST: ⚠ **欠損値・空配列・知らない値**の扱いを確かめる。
- MUST: ⚠ **推定値と確定値を取り違えないこと**を確かめる。
- SHOULD: ⚠ **DOM 無しで確かめられる形**にする（⚠ `photos.js` などと同じ作り）。

## 画面

⚠ **測り方は [`ui-ux-review`](../skills/ui-ux-review/SKILL.md) が正本。**

- MUST: ⚠ **見えているだけでなく、⚠ 実際に操作できること。**
- MUST: ⚠ **目盛りやラベルの操作が、⚠ 背面の地図へ抜けないこと。**

## 直したとき

- MUST: ⚠ **直した不具合は、検査として残す**（正本は `CLAUDE.md` §2）。
- MUST: ⚠ **変更前に落ちて、⚠ 変更後に通ること**を確かめる。

## ⚠ 演習が、世界を変えてはいけない

⚠ **これは `hidetzu/claude-dev-template` から戻したもの**（2026-09-04。Owner 判断）。
⚠ **今昔ではまだ踏んでいない。**⚠ **踏める口があるので、⚠ 踏む前に置く。**

⚠ **危ないのは「稽古のつもりだった」という思い込みのほう。**
⚠ **本物だと思っていれば、⚠ そもそも触らない。**

- MUST: ⚠ **外へ出る呼び出しは、⚠ 何かが明示的に塞いでいない限り、⚠ 本物として扱う。**
  ⚠ **「検査っぽいから大丈夫」ではない。**⚠ **塞いだものを指させること。**
- MUST NOT: ⚠ **フラグや名前を、⚠ 副作用が無い約束として読まない。**
  ⚠ **記録の書き先をすげ替えることと、⚠ することをすげ替えることは別。**
  ⚠ **どちらなのかは、⚠ スイッチの名前では分からない。**
- MUST: ⚠ **検査だからといって、⚠ 触ってよい範囲を広げない。**
  ⚠ **逆。**⚠ **検査は、⚠ 本番より狭くしか触れない。**
- SHOULD: ⚠ **塞がっていることを、⚠ 通らなかった口を見て確かめる**
  （⚠ 早く return する行を読むのは、⚠ その行の証明であって、⚠ 走りの証明ではない）。
  ⚠ **同時に、⚠ 通る側も 1 本置く。**⚠ **でないと、⚠ 起動に失敗しただけで通る**
  （⚠ **落ちた ≠ 狙った理由で落ちた**）。

### ⚠ 今昔で、外の状態を動かす口（⚠ 2026-09-04 実測）

```text
.claude/hooks/ask-slack.mjs        chat.postMessage   ⚠ すげ替える口が無い。⚠ 走らせたら本当に投稿される
.claude/hooks/slack-doctor.mjs     --post のときだけ  ⚠ 同上
gh pr create / gh pr merge         GitHub            ⚠ 人の許可が要る（`CLAUDE.md` §8）
git push / wrangler deploy         GitHub / 配信      ⚠ 同上
```

⚠ **いま検査が実際に走らせているのは `telemetry.mjs` だけ。**
⚠ **`CLAUDE_PROJECT_DIR` ごと仮の場所へ向けている**（`test/check/eval.mjs`）。
⚠ **あれは副作用が記録だけなので、⚠ 書き先をすげ替えれば足りる。**
⚠ **`ask-slack.mjs` は同じ形にならない。**⚠ **読むだけにしてある。**

## 増やしすぎない

- MUST NOT: ⚠ **実装の手順だけを固定する検査を、大量に足さない。**
- SHOULD: ⚠ **利用者から見た契約・Domain の契約・入出力**を中心に見る。
