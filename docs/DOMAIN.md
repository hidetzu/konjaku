# 今昔 — ドメインモデル（言葉の持ち主）

⚠ **この文書は「その言葉が何を指すか」だけを持つ。**

| 文書 | 何を持つか |
|---|---|
| **この文書** | ⚠ **言葉の定義。**何をそう呼ぶか。画面に出す語と、出さない語 |
| [`SPEC.md`](SPEC.md) | いま**何が言えるか**。件数・割合・分母 |
| [`adr/`](adr/) | ⚠ **なぜそう決めたか。**経緯 |
| [`../CLAUDE.md`](../CLAUDE.md) | どう作業するか |

⚠ **重複させない。**件数はここに書かない（`SPEC.md` が持つ）。
理由はここに書かない（`adr/` が持つ）。

---

## 1. 土地の答え — 3 つの層

利用者の問いは 1 つ（**この土地は、昔なんだったのか？**）。
答えは**根拠の種類**で 3 つに分かれ、**確実性の高い順**に出す（[ADR 0030](adr/0030-土地の答えは確実性の高い順に3層で出す.md)）。

| | ⚠ **層が答える問い** | 根拠 | 出す条件 |
|---|---|---|---|
| 1 | **ここは、どういう土地？** | 地形分類 | ⚠ **常に** |
| 2 | **昔は、何があった？** | 明治期の低湿地データ | データがあるとき |
| 3 | **いま建っている建物は、何の上？** | 取り込み済みの建物の足元 | 建物があるとき |

⚠ **`第1層` `第2層` `第3層` は内部の呼び名。**画面に出さない。
⚠ **順序は重要度ではない。**根拠の種類と、解釈の順序。

---

## 1-2. ⚠ それぞれの根拠を、どこから取っているか（＝出典）

⚠ **「根拠がある」と言えるのは、出どころを名指しできるから。**
⚠ **名指しできないものは、根拠ではない。**

| 層 | 何を取るか | 取得元 | 取り方 | いつ取るか |
|---|---|---|---|---|
| **1** | 成因（36 区分） | 国土地理院 ⚠ **地理院地図 ベクトルタイル提供実験**<br>`experimental_landformclassification1` | 図式コードを**直読み** | 閲覧時 |
| **1** | 人の手（盛土地･埋立地・切土地） | 同上 `experimental_landformclassification2` | 同上 | 閲覧時 |
| **1** | 成因・災害リスクの**説明文** | ⚠ **国土地理院の記述をそのまま** | 引用 | 事前 |
| **2** | 明治期の低湿地（14 区分） | 国土地理院 `swale`（ラスタ） | ⚠ **画素の色を読む** | 閲覧時 |
| **2** | 区分名と色の対照表 | 地形図図式詳解（陸地測量部）／広辞苑第四版（岩波書店） | 事前に表へ | 事前 |
| **3** | 建物の形と高さ | **OpenStreetMap**（ODbL）／ 一部 Overpass | 事前取り込み ＋ 実行時 | 両方 |
| **3** | 建物の足元の区分 | ⚠ **第2層のラスタを、建物の重心で読む** | 画素 | 事前 ＋ 実行時 |
| — | 空中写真（年代） | 国土地理院 `seamlessphoto` `gazo1`〜`gazo4` `ort_old10` `ort_riku10` | タイル | 閲覧時 |
| — | 標高 | 国土地理院 `dem` `dem5a` | タイル | 閲覧時 |
| — | 年つきの事物 | **Wikidata** | 事前取り込み | 事前 |
| — | 住所検索 | 国土地理院 住所検索 API | 実行時 | 閲覧時 |

⚠ **通信先は 2 ホストだけ**（`cyberjapandata.gsi.go.jp` ／ `maps.gsi.go.jp`）＋ Overpass。
⚠ **増やしたら検査が落ちる**（`check.mjs`「公開物が名指ししている外部の相手」）。

### ⚠ 出典で気をつけること

- ⚠ **「提供実験」を「実験的で当てにならない」と読ませない。**
  提供の形式の話であって、⚠ **データの確からしさの話ではない**
- ⚠ **OpenStreetMap は ODbL。**⚠ **クレジットは、OSM を実際に載せているソースから出す**
- ⚠ **説明文は国土地理院の記述をそのまま使い、こちらで書かない**
  （[ADR 0007](adr/0007-根拠のないことは書かず断定は引用にする.md)）
- ⚠ **事前に取り込んだものと、閲覧時に取るものを混ぜない。**
  ⚠ **同じ土地でも答えが変わりうる**（[ADR 0021](adr/0021-同じ問いに答える実装を2つ持たない.md)）

⚠ **件数・割合・カバー範囲は [`SPEC.md`](SPEC.md) が持つ。**ここには書かない。

---

## 2. 語（⚠ 画面に出す語と、出さない語）

### 2-1. 第1層のもの

| 概念 | ⚠ **画面に出す語** | ⚠ **画面に出さない語**（出典・`/about`・コードでだけ） |
|---|---|---|
| 場所の指し方 | ⚠ **`この土地は`** | ⚠ `この場所は` は**使わない** |
| 成因 | 区分名そのまま（`旧水部` `扇状地` `氾濫平野・海岸平野` …） | `地形分類` `土地条件` |
| 人の手が入ったこと | ⚠ **`人の手で ○○ になっています`** | `人工地形` `artificial` |

⚠ **第1層で時間の語（`もとは` `昔は` `だった`）を使わない。**
⚠ **第1層は、いまの土地の成り立ち。**さかのぼるのは第2層で、混ざると層に分けた意味が消える。

⚠ **成因と人の手は、行を分ける。**1 行に混ぜると「もとから埋立地だった」に読める。

⚠ **この 3 つを組み立てるのは `public/words.js` の `ground1Lines()` ただ 1 つ**（2026-08-20）。
⚠ **トップ・`/peel` の情報パネル・共有カードの文が、全部そこから出る。**

```
public/words.js   ⚠ **語と、行の割り方を決める。**⚠ 太字にするかは知らない
トップ / /peel    ⚠ **置くだけ。**⚠ 太字にするかだけを決める
public/verify.js  ⚠ **narrate() の 1 文目も、ここから借りる**（共有カードの文になる）
```

⚠ **寄せる前は、同じ第1層が画面ごとに違う形で出ていた**（`main` = `c8beb88` の実測）。

```
豊洲 トップ   もとは 水だった土地（旧水部）です。いまは 盛土地･埋立地 です。
札幌 トップ   この場所は 扇状地 です。
豊洲 /peel    この土地は 旧水部 ／ 人の手で 盛土地･埋立地 になっています
```

### ⚠ 2-1-1. 問いの見出し

⚠ **層の名前は、その層が答えている「問い」で呼ぶ**（[ADR 0029](adr/0029-画面の言葉は利用者の問いから決める.md)）。

```
第1層  ここは、どういう土地？
第2層  昔は、何があった？
第3層  いま建っている建物は、何の上？
```

⚠ **字は `public/words.js` の `layerTitle()` ただ 1 つ。**⚠ **トップと `/peel` が同じものを出す。**
⚠ **トップは 2026-08-20 まで見出しを出していなかった。**
⚠ 実測（利用者役 4 名・375×667・画面だけを見せた。⚠ **実在の利用者ではない**）:
⚠ **4/4 が「見出しあり」を選び、⚠ 2 名が「見出しが無いと、`明治期 低湿地データ` が見出しに見える」**と答えた。

⚠ **見出しを足すと 26px 増える。**⚠ **足すだけでは答えが画面から出る**ので、
⚠ **答えを画面の先頭へ動かした**（実測 375×667・`hasTouch`: 答えの下端 659 → 258）。

### 2-2. 第2層のもの

| 概念 | 画面に出す語 |
|---|---|
| 資料そのもの | `明治期の低湿地データ` |
| 時代 | ⚠ **`明治期（1868〜1912年）`**（⚠ **資料の年を作らない**） |
| 区分 | 区分名そのまま（`河川・湖沼・海面` `田` `荒地` …） |

### 2-3. 第3層のもの

| 概念 | 画面に出す語 |
|---|---|
| 建物が建っている地面 | ⚠ **`足元`** |
| 建物 1 件ずつの話であること | `建物ごと` |

---

## 3. 取得の結末（⚠ 3 つを混ぜない）

⚠ **同じ語を 2 つの状態に使わない**（[ADR 0029](adr/0029-画面の言葉は利用者の問いから決める.md)）。

| 内部の名前 | 何が起きたか | ⚠ 画面で言うこと | ⚠ 言ってはいけないこと |
|---|---|---|---|
| `ok` | 読めた | その値 | — |
| `ok` かつ 0 件 | 読めて、該当が無い | `記録なし` | ⚠ `判定できません` |
| `absent`（404） | ⚠ **その資料が、この土地を対象にしていない** | ⚠ **`整備対象外`**（⚠ 1 語に決める） | ⚠ **`無い`・`存在しない`** |
| `unreachable` | ⚠ **こちらが読めなかった**（通信断・時間切れ・拒否） | `読み込めませんでした` `いま出せません` | ⚠ **`無い`・`データなし`** |
| `notyet` | ⚠ **こちらがまだ用意していない** | `この場所ではまだ提供していません` | ⚠ **`取得できませんでした`**（回線のせいに読める） |

### 3-2. ⚠ 写真が「いま届いたか」（⚠ **上の 5 つとは別の軸**）

⚠ **上の 5 つは「その資料がこの土地を対象にしているか」。**
⚠ **こちらは「いま画面に届いたか」。**⚠ **混ぜない。**

| 内部の名前 | 何が起きたか | ⚠ 画面で言うこと |
|---|---|---|
| `ok` | 届いた | ⚠ **何も言わない**（出ているのが当たり前） |
| `pending` | 猶予（1.2 秒）の中 | ⚠ **何も言わない**（言うと読み込みのたびに光る） |
| `late` | ⚠ **猶予を過ぎても届かない。理由は知らない** | `まだ出ていません` |
| `fail` | ⚠ **落ちたのを実際に観測した** | `読み込めませんでした（理由）` |

⚠ **`late` と `fail` を混ぜない。**
⚠ **`late` は「まだ来ていない／404 で写真が無い」を含み、こちらは理由を知らない。**
⚠ **知らないことを「読み込めませんでした」と書かない。**

⚠ **404 は `fail` にならない。**⚠ **MapLibre は 404 を異常と見なさず、`<img>` からも理由が取れない。**
⚠ **「遅い」と区別できないので、`late` に留める。**

⚠ **接続の話は、こちらが知っている範囲でしか言わない。**
⚠ `navigator.onLine === false` のときだけ「インターネットに接続していません」と言い切り、
⚠ `true` のときは「接続を確認してください」に留める。

⚠ **層を 3 つに分けてある**（2026-08-20）。⚠ **越えない。**

```
public/photos.js   ⚠ **状態を決める。**⚠ **画面へ出す字を持たない**
public/words.js    ⚠ **字を決める。**⚠ **状態の作り方を知らない**
画面（top.js / peel3d.js）
                   ⚠ **置くだけ。**⚠ **何も判断しない**
```

⚠ **相手先の振る舞いが変わっても、直すのは `photos.js` の 1 か所。**
⚠ **言い方を変えるなら、直すのは `words.js` の 1 か所。**

---

### ⚠ 3-2-1. 「この地点で選べる段」も、⚠ 1 か所（2026-08-23。hidetzu/konjaku#170）

⚠ **同じ問いに、⚠ 2 つの実装が答えていた。**

```
トップ   index.html の buildFrames   ⚠ ページの中に直書き
/peel    peel3d.js の stepsFrom
```

⚠ **すでに 1 か所ずれていた**（実測 2026-08-23・`main` = `9b6e83b`）。

| | 明治期を段に足す条件 | ⚠ 札幌（整備対象外）のものさし |
|---|---|---|
| トップ | ⚠ **判定できたときだけ** | — |
| /peel | ⚠ **無条件** | ⚠ **右端が「明治期」**（⚠ 押しても水域は出ない） |

⚠ **層を 3 つに分けてある。**⚠ **越えない。**

```
public/eras.js     ⚠ **段を決める。**⚠ **並びも字も持たない**
public/words.js    ⚠ **字を決める**（⚠ 復元できなかったときの言い方）
画面（top.js / peel3d.js）
                   ⚠ **並べるだけ。**⚠ **落とし方を知らない**
```

⚠ **並びはモデルが決めない**（⚠ **古い順に返す**）。⚠ **向きは呼ぶ側**
（⚠ コマ帯は左から明治期、⚠ ものさしは左が現在。⚠ **見せ方が違う**）。

⚠ **時間座標（tau）もモデルが持たない。**⚠ **`/peel` の 3D 固有**
（⚠ 建物が消える年・水位・フェードがこれで決まる。⚠ **段を間引いても動かさない**）。

---

## ⚠ 3-3. 土地情報を「取ってくる」と「控える」を分ける

⚠ **2 つの画面が、同じ土地の同じ答えを別々に取りに行っていた**（2026-08-20 に直した。
hidetzu/konjaku#121）。

⚠ **実測**（2026-08-20・`main` = `d410455`・豊洲・375×667・Service Worker 無効）

```
トップ              地形分類 2 本 ／ 明治期ラスタ  5 本 ／ 標高 1 本 ／ 建物 1 本
/peel（トップ経由）  地形分類 2 本 ／ 明治期ラスタ 43 本 ／ 標高 0 本 ／ 建物 5 本
⚠ **同じ座標で、地形分類をもう一度取っていた**（トップで取り終えているのに、/peel が知らない）
```

⚠ **層を 3 つに分けてある。**⚠ **越えない。**

```
public/verify.js   ⚠ **外から取ってくる。**⚠ **控えることを知らない**
public/land.js     ⚠ **取得済みを控える、ただ1か所。**⚠ **取り方を知らない・字を持たない**
画面（top.js / peel3d.js）
                   ⚠ **置くだけ。**⚠ **取得の層を直接呼ばない**
```

⚠ **控えるときの決まり**

| | ⚠ どうする | ⚠ なぜ |
|---|---|---|
| キー | ⚠ **小数 5 桁の `lat,lon`** | ⚠ **URL の `ll` と同じ粒度。**★ の 4 桁は「同じ場所とみなす丸め」で、⚠ **別の問い** |
| 置き場 | `sessionStorage`（`konjaku:land:v1:`） | ⚠ **画面遷移で JS のメモリは消える。**版を持たせて、形が変わったら混ざらない |
| ⚠ **取れなかった回** | ⚠ **控えない** | ⚠ **控えると、一度つながらなかった土地が、その先ずっと「取れない土地」になる**（掟: 取得できなかった ≠ 存在しなかった） |
| 「資料の範囲外」 | ⚠ **控える** | ⚠ **それは答えであって、失敗ではない** |
| 保存が使えない | ⚠ **控えないだけ。画面は止めない** | Safari のプライベート・容量超過・埋め込み枠での遮断 |
| 壊れた控え | ⚠ **無かったことにして取りに行く** | ⚠ **例外を投げて画面を白くしない** |

### ⚠ 3-3-1. 明治期の「面」も、同じ 3 層に乗せた（2026-08-20）

⚠ **`/peel` の画面コードの中に、⚠ 取得・分類・集計・キャッシュが 74 行あった。**
⚠ **3 つめのキャッシュでもあった**（`land.js` の inflight ／ `verify.js` の imgCache と別）。

```
public/verify.js   swaleTiles / swaleTile / readSwaleTile   タイル束と canvas の画素読み
                   swalePixel(lon,lat)   1 画素だけ         ⚠ **点。建物ごとに何百回も呼ぶ**
                   swaleArea(bbox)       mask ＋ 集計        ⚠ **面。GeoJSON は作らない**

public/land.js     areaKey / areaSummary / meijiArea(bounds)
                   ⚠ **控えるのは集計だけ。**⚠ **mask は控えない**（豊洲 1.25MB）
                   ⚠ **点（meijiPoint）とはキーが別**（ADR 0030）

public/peel3d.js   buildWater(bbox)      mask → 矩形 → GeoJSON → MapLibre
                   ⚠ **矩形化は「どう描くか」の都合。**⚠ **水かどうかは判定しない**
```

⚠ **点と面は別の問い**（ADR 0030）。⚠ **点の結果から面を推測しない。**⚠ **面を点の代用にしない。**

⚠ **`swalePixel()` と `meiji()` を混ぜない。**
⚠ `meiji()` は根拠つきの「事実」（近傍 15×15 の一致率・200m 四方の集計・出典）を組む。
⚠ **1 地点について 1 回だけ呼ぶもの。**⚠ **建物ごとに呼ぶと、近傍計算と集計が件数ぶん走る。**

⚠ **`tiles{ok, absent, unreachable}` の分け方を潰さない。**
⚠ **1 枚も読めていないのに「データがありません」と書くことになる**（掟の一行目）。
⚠ 実際に確かめた: 403 を `absent` に丸めると、画面が
「このエリアには明治期の低湿地データがありません。」に変わる。

---

⚠ **使い回すのは トップ → `/peel` の向きだけ**（2026-08-20 の作りきりの範囲）。
⚠ **逆向き（`/peel` を先に見てからトップへ戻る）は、いままでどおり取り直す。**
⚠ **`facts()` の組み立て（`unread` の判定）は `verify.js` が持っており、
控えから組み直すと、⚠ 同じ判断が 2 か所になる**（掟: 同じ問いに答える実装を2つ持たない）。
⚠ **だから、まとめ取りの側は控えを読まずに書くだけにしてある。**

---

⚠ **`absent` と `unreachable` と `notyet` は、原因の持ち主が違う。**
相手の資料の範囲 ／ 通信 ／ こちらの都合。⚠ **どれか 1 つに寄せない。**

⚠ **`⚠` は危険（災害リスク）に使う。**在庫や対応状況に使わない。

---

## 4. データモデル

⚠ **層を値として持つ。**⚠ **文字列を画面ごとに組み立てない。**

```
layersOf(...) → { layers: [ Layer, ... ], missing: [ Missing, ... ] }
```

⚠ `layers` は必ず層の番号順。立たない層は入らない。

**`Layer`** — `n`（内部）／ `title`（層の問い）／ `head` ／ `what` ／ `den` ／ `subs[]`

⚠ **`head` は 3 種に分ける**（1 つの文字列にしない）。

| 種 | 中身 | ⚠ 決まり |
|---|---|---|
| `pct` | 数字 | ⚠ **`den`（分母）が無ければ作れない** |
| `name` | 区分名 | 押せる（意味を出す） |
| `none` | 出せない | ⚠ **数値を作らない**（0% を出さない） |

**`Missing`** — `n` ／ `why`（`unread` / `outside` / `notyet`）／ `say`

⚠ **出ない層も、その層の位置に置く。**黙って消すと「その土地に何も無い」に読まれる。
⚠ **`say` は `WORD` が持つ。**ここで文を作らない。

---

## 4-2. ⚠ 画面の幅（⚠ **狭い幅を既定にする**）

⚠ **狭い幅が既定。**⚠ **広い幅だけ `min-width` で足す。**

```
⚠ 新しく `max-width` を書かない
```

⚠ **なぜ。**⚠ **`max-width` で書くと、狭い画面が「例外」になる。**
⚠ **この作品は、⚠ 狭い画面で使われる**（実測の基準幅は 375×667）。
⚠ **既定のほうを例外として書くと、⚠ 足すたびに広い側の打ち消しが要る。**

### ⚠ いまの実測（2026-08-21・`main` = `bc1c156` から反転したあと）

| ファイル | `min-width` | ⚠ `max-width` |
|---|---|---|
| `public/index.html` | 4 | ⚠ **0** |
| `public/css/tokens.css` | 0 | ⚠ **0** |
| `public/peel.html` | 1 | ⚠ **0** |

⚠ **`@media (hover:none)` は対象外**（⚠ **幅ではなく入力手段**の話）。

### ⚠ `public/peel.html` を反転した（2026-08-21）

⚠ **`@media(max-width:680px)` の 112 組を、⚠ 素の指定と `@media(min-width:681px)` へ振り分けた。**

```
反転前  狭い側 37 セレクタ / 112 組 ／ 広い側  8 セレクタ / 14 組
反転後  狭い側 0            ／ 広い側 31 セレクタ / 73 組
⚠ unset / initial / revert   0 組   ← ⚠ **帳尻合わせをしていない**
```

⚠ **6 幅（375×667 / 344×882 / 320×640 / 1280×800 / 1440×900 / 1920×1080）で、
⚠ 見えている字も主要な器の位置も 1px も変わっていない。**

⚠ **順番が効く。**⚠ **`@media(min-width:681px)` は、⚠ 素の指定より後ろに置く。**
⚠ **前に置くと、狭い幅の値が広い画面でも勝つ**（実測で踏んだ）。

⚠ **数えるときは、⚠ CSS のコメントを先に落とす。**
⚠ 落とさないと、⚠ **コメントの中の `{` を規則の始まりとして拾う**（狭い側が 37 → 16 に見えた）。

⚠ **`#ruler`（スマホのものさし）と `#track`（PC の横棒）は、⚠ いまも別の道具**
（`docs/SPEC.md` §23）。⚠ **置き方はそれぞれの側が持つ。**

---|---|---|
| `public/index.html` | 4 | ⚠ **0** |
| `public/css/tokens.css` | 0 | ⚠ **0** |
| `public/peel.html` | 1 | ⚠ **1** |

⚠ **`@media (hover:none)` は対象外**（⚠ **幅ではなく入力手段**の話）。

### ⚠ 残している 1 箇所（`public/peel.html` の `max-width:680px`）

⚠ **これは直さない。**⚠ **理由を書く。**

⚠ **`max-width:680px` と `min-width:681px` は相補の対**で、⚠ **狭い側と広い側**を分けている。

```
狭い側（max-width:680px）  110 組（セレクタ × プロパティ）
広い側（min-width:681px）    6 組
⚠ 狭い側にしか無い          104 組
   うち display だけ         12 組（反転が機械的）
   ⚠ **値の付け替え**        92 組（⚠ **機械的でない**）
```

⚠ **狭い側を素の指定へ移すと、⚠ その 104 組が広い画面にも効く。**
⚠ **広い側へ 92 組の打ち消しを書くことになり、⚠ 「狭い幅が 1px も変わらない」を保てない。**

⚠ **`#ruler{display:block}`（スマホの「ものさし」）と `#track{display:none}`（PC の横棒）は、
⚠ 年代を選ぶ道具を分けている核心**
（⚠ **置き方は PC / スマホそれぞれのブロックが持ち、共有の既定には書かない。**
⚠ **片方を触ってもう片方を壊した実測がある**。`docs/SPEC.md` §23）。

⚠ **`/peel` の CSS を作り直すときに、一緒に直す。**⚠ **それまでは残す。**
⚠ **ただし増やさない**（`test/check.mjs` が見張る）。

---

## 5. 言葉の持ち主（⚠ 実装のどこにあるか）

⚠ **同じ問いに答える実装を 2 つ持たない**（[ADR 0021](adr/0021-同じ問いに答える実装を2つ持たない.md)）。

| 言葉 | 持ち主 |
|---|---|
| 出どころの語彙（実測／未取得／欠落／未対応／推定） | `public/prov.js` |
| `/peel` の答えと出どころ | `peel3d.js` の `WORD` |
| トップの根拠カードと導線 | `top.js` の `TOPWORD`（⚠ 2026-08-24 に `index.html` から出した） |
| ⚠ **写真が「いま届いたか」** | ⚠ **`public/photos.js`**（状態）＋ `public/words.js`（字） |
| 明治期の 14 区分 | `public/swale.js` |
| 年代の定義 | `public/verify.js` |
| ⚠ **この地点で選べる段** | ⚠ **`public/eras.js`**（⚠ 段の集合と、⚠ 復元の種類） |
| ⚠ **同じ土地について「もう取ってあるもの」** | ⚠ **`public/land.js`** |

⚠ **ここに書き写さない。**上の 6 か所が正で、この表は**在りかの索引**。
