# 0068. 広い幅では、保存した場所から入って、根拠を開いたまま読む

- 状態: 採用（⚠ **決めたのは Owner。**⚠ 2026-08-29）
- 日付: 2026-08-29

⚠ **`docs/adr/0048` の「試す順」の 2（⚠ PC で掘る）。**
⚠ **`docs/adr/0064` は「PC 向けの別画面は入れない」と書いていた。**⚠ **ここで範囲を広げる。**

## ⚠ 測ってから決めた

⚠ **実測（2026-08-29・1280×950）**: ⚠ **スマホの形が、⚠ そのまま横に伸びているだけだった。**

```text
⚠ **答えの 1 文が 1238px 幅の 1 行**   ⚠ 読む行として長すぎる
⚠ **年代が 1 つ 173px**                ⚠ スマホでは 44px
⚠ **検索窓が 1264px**
⚠ **右側の空きが 8px**                 ⚠ 横に置ける場所が無い
⚠ **板は横いっぱい・縦 318px**          ⚠ **深掘りの余地が縦にしか無い**
```

## ⚠ 3 案を絵で比べた

| | 何が変わるか | ⚠ 何が変わらないか |
|---|---|---|
| A 右に板 | 地図が主役のまま。⚠ 板が読める幅 | ⚠ **中身は同じ** |
| ⚠ **B 左に一覧・右に板** | ⚠ **保存した場所から入れる** | ⚠ **中身は同じ** |
| ⚠ **C 上下 2 段** | ⚠ **根拠が一度に読める**（⚠ 開く操作が要らない） | ⚠ **中身は同じ** |

⚠ **3 案とも、⚠ 中身は同じものを並べ替えているだけ。**
⚠ **`docs/adr/0048` の「深く・広く・時間をかける」のうち、⚠ 並べ替えで届くのは 2 つだけ。**

```text
⚠ **広く**        ⚠ B の一覧（⚠ 保存した場所を横に並べ、⚠ そこから入る）
⚠ **時間をかける** ⚠ C（⚠ 開く操作なしで根拠が全部読める）
⚠ **深く**        ⚠ **届かない。**⚠ **材料を増やすか、⚠ 見せ方を変える必要がある**
```

⚠ **B ＋ C を採った**（⚠ Owner 判断）。⚠ **「深く」は、⚠ 器を作ってから決める。**

## 決めたこと

```text
⚠ **1400px から切り替える。**⚠ **その手前では、⚠ 狭い幅のまま**（⚠ 中途半端に 2 列にしない）

左     保存した場所 16rem（256px）   ⚠ 立てたまま。⚠ 閉じるボタンは出さない
真ん中 地図                          ⚠ **主役**
右     板 22rem（352px）＋ 出典       ⚠ **根拠は最初から開いている**
```

⚠ **1 件も保存していないときは、⚠ 左を空けない**（⚠ 地図が広がる）。
⚠ **狭い幅では、⚠ いままでどおり**（⚠ 押すと出る器のまま。⚠ 散歩中は 3 名中 2 名が「その場では読まない」）。

## ⚠ 切り替え点を 1 度上げた

```text
⚠ **1200px で切り替えると、⚠ 地図 560px < 左右の合計 672px**
⚠ **地図が主役でなくなる**（`.claude/rules/domain.md`）
→ ⚠ **1400px にした**（⚠ 地図 784px > 608px）
```

⚠ **検査が捕まえた。**⚠ **「地図が、⚠ 左右に立てたものより広いこと」を主張に入れてあったから。**

## ⚠ 1400 が 2 か所にある

⚠ **CSS の `@media` と、⚠ JavaScript の `matchMedia`。**
⚠ **一覧を出したままにするか・根拠を開くかは JavaScript が持つ状態**（`hidden` / `open`）
⚠ **なので、⚠ 幅を両方が知る必要がある。**

⚠ **`CLAUDE.md` §3 のとおり、⚠ 機械で突き合わせる検査を足した**（`test/check/next.mjs`）。
⚠ **ずれると、⚠ 「見た目は 2 列なのに一覧が出ない」という、⚠ 落ちない不具合になる。**

## ⚠ 決めていないこと

```text
⚠ **「深く」を何にするか**
   ⚠ **並べ替えでは届かない。**⚠ **いま v0.1.0 が持っている材料はこれで全部**:
     足元の区分／明治期の低湿地／空中写真 7 年代／周辺の公式記録（⚠ 2 地域）／保存した場所
   ⚠ **材料を増やすか、⚠ 見せ方を変えることになる**
     （⚠ 例: 保存した場所を地図に一度に出す・⚠ 年代を 2 つ並べて比べる・⚠ 標高を足す）
   ⚠ **どれも範囲の話。**⚠ **器を実機で見てから決める。**
⚠ **タブレット（768〜1399px）をどうするか**（⚠ いまは狭い幅のまま）
```

---

## ⚠ 取り消した（⚠ 2026-08-29。⚠ 同じ日のうちに）

⚠ **3 列をやめた。**⚠ **スマホと同じ形を、⚠ そのまま中央に立てるだけにした**（⚠ Owner 判断）。

### ⚠ なぜ

⚠ **Owner が実機で「操作しづらい」と言った。**⚠ **測ったら、⚠ 数字に出ていた。**

| | ⚠ 押せるものの散らばり | ⚠ いちばん離れた 2 つ |
|---|---|---|
| スマホ | 302 × 594px | 615px |
| ⚠ **3 列** | ⚠ **1250 × 637px** | ⚠ **1374px** |
| ⚠ **中央に 1 本** | ⚠ **310 × 877px** | 892px |

⚠ **利用者役 3 名とも「操作しづらい」側の答え**（⚠ 「目が左右に振られる」「押すものが遠い」
⚠ 「スマホで覚えた場所と違うので、⚠ 一度探し直した」）。

⚠ **3 案を見せたら、⚠ 3 名とも中央に 1 本を選んだ。**
⚠ **この一連のテストで、⚠ 3 名が同じものを選んだのは初めて。**

```text
⚠ **板を下に横いっぱいへ戻す案**も測った（⚠ 3 列にする前の形）。
⚠ **答えの 1 文が 1398px の 1 行になる。**⚠ **中央に立てると 342px**（⚠ スマホは 333px）。
```

### ⚠ 不具合を 1 つ出していた

⚠ **3 列にしたとき、⚠ 検索の候補が「保存した場所」の柱の裏に入った。**
⚠ **候補の 1 つ目を触ると、⚠ 柱の見出しが返ってきた。**⚠ **押せなかった。**

```text
⚠ **原因**: ⚠ `.saved` に `z-index:20`、⚠ `#hits` に指定なし
⚠ **検索は「どこを見るか」を決める操作。**⚠ **覆われると先へ進めない**
```

⚠ **Owner が実機で見つけた。**⚠ **こちらの実描画は捕まえていなかった。**
⚠ **重なりを見るだけでは足りず、⚠ 実際に触って候補が返るかまで見る検査を足した。**

### ⚠ 残したこと

```text
⚠ **保存した場所の柱は立てない。**
   ⚠ **トップは「どこを見るか」を決める画面。**⚠ **一覧は深掘りの入口。**⚠ **分ける**
⚠ **JavaScript は幅を見ない。**⚠ **見せ方は CSS の 1 か所**
   ⚠ **3 列のときは 2 か所に切り替え点を持ち、⚠ 突き合わせる検査を置いていた。**
   ⚠ **持たないほうが強い。**⚠ **「持たないこと」を見る検査に入れ替えた**
```

⚠ **`docs/adr/0068` の「深く」の話（⚠ 何を足すか）は、⚠ 深掘り画面のほうへ移った。**
