# 0096. 文字の大きさは、上位 5 段だけを theme.css に集める

- 状態: 決めた
- 日付: 2026-09-05
- ⚠ **hidetzu/konjaku#455。**⚠ **Owner 判断。**

## 何が起きていたか（⚠ 実測 2026-09-05・`66cb7e9`）

⚠ **色は 1 か所に集まった**（`theme.css`）。⚠ **文字は直書きのままだった。**

```text
--fs- の数   0
種類        15
のべ       143 か所
上位        .8125rem 37 ／ .75rem 29 ／ .875rem 21 ／ .9375rem 16 ／ 1rem 14
```

⚠ **数え方**: `grep -hoE "font-size:[^;}]+" public/*.css`
（⚠ **`}` で終わる書き方を落とさないと 18 種に見える**）。

⚠ **4 日で増えていた**（⚠ 2026-09-01 → 2026-09-05）。

```text
.8125rem  30 → 37
.75rem    21 → 29
.875rem   14 → 21
.9375rem  13 → 16
CSS       7 枚 → 9 枚
```

## 何を決めたか

⚠ **上位 5 つだけを段にする**（Owner 判断）。⚠ **`theme.css` の 1 か所に置く。**
⚠ **名前は「本文からの距離」。**⚠ **値ではなく意味で名づける**（`.claude/rules/css.md`）。

```text
--text-body   1rem      16px   ⚠ 14 か所（基準）
--text-sm     .9375rem  15px   ⚠ 16 か所
--text-sm2    .875rem   14px   ⚠ 21 か所
--text-xs     .8125rem  13px   ⚠ 37 か所
--text-xs2    .75rem    12px   ⚠ 29 か所
```

⚠ **`sm2` / `xs2` は「段が足りていない」印でもある**（Owner）。
⚠ **増やす前に、⚠ いまの段で足りないかを見る。**

## ⚠ 役割（`--fs-*`）は、⚠ まだ足さない

⚠ **ここは段（大きさ）だけ。**
⚠ **名前は、⚠ 同じ意味のものが 2 つ現れてから足す**（`.claude/rules/css.md`）。

## ⚠ 残した直書き

```text
.6875rem 11 ／ 1.375rem 4 ／ 1.25rem 2 ／ 1.0625rem 2 ／ .625rem 2
2rem 1 ／ 100% 1 ／ 1.75rem 1 ／ 1.625rem 1 ／ 1.125rem 1     計 26 か所
```

⚠ **決めた範囲の外。**⚠ **検査も、⚠ ここは落とさない。**

## ⚠ 見た目は 1px も変えていない

⚠ **前後で、⚠ 全要素の `font-size` を突き合わせた。**

```text
画面    7 つ × 2 幅 ＝ 14
要素    1829 個
違い    ⚠ 0 個
```

## 検査

⚠ **`test/check/next.mjs` ㉕。**⚠ **見るのは 2 つ。**

```text
段が theme.css の 1 か所に在ること
段に在る値を、⚠ 直書きで書いていないこと
```

⚠ **わざと壊して 4 本とも、⚠ 狙った理由で落ちることを確かめた**
（⚠ 直書きを足す ／ 1 か所だけ戻す ／ 段を減らす ／ 空白つきで書く）。

## ⚠ hidetzu/konjaku#456 の検査も、⚠ 一緒に足した

⚠ **hidetzu/konjaku#456 は「再現しない」で Close した**（2026-09-05）。
⚠ **だが余裕は縮んでいる。**⚠ **実測（本番・`66cb7e9`）**:

```text
板 240〜270px ／ 地図の半分 320px   ⚠ 差は 50〜80px
```

⚠ **`layoutAim()` は「地図の半分」を上限にしている。**
⚠ **板がそれを超えると、⚠ 印は縦中央で止まり、⚠ そこから先は隠れる。**
⚠ **板をあと 50px 伸ばしたら止まる形にした。**

⚠ **わざと壊して 4 本とも落ちることを確かめた**
（⚠ 板を半分より高くする ／ 印を押し込む ／ 字を押し込む ／ 印を消す）。
⚠ **2 本は、⚠ 壊しても実際には隠れなかったので数えていない**（⚠ 中央でも板より上だった）。
