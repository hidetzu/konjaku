# 0057. 地形分類の「整備範囲外」は、粗くなるのであって無くならない

- 状態: 採用（⚠ **決めたのは Owner。**⚠ 2026-08-29）
- 日付: 2026-08-29

⚠ **`docs/adr/0056` が「次に測る」と書いたものの答え。**
⚠ **答えは「③ は在るが、⚠ 形が違う」。**

## 測ったこと（⚠ 実測 2026-08-29・10 地点）

```bash
node scripts/survey-landform-coverage.mjs
```

⚠ **数字は書き写さない。**⚠ **判断が変わったところだけ書く。**

```text
⚠ **詳細版（z14〜16）が 1 枚も無い土地がある**
   ⚠ 稚内・石垣・上高地・小笠原・佐渡・十津川 ── ⚠ **z16 は全部 404**
⚠ **ただし、⚠ そこでも広域版（z13）は取れる**
⚠ **部分的に穴が空く土地もある**（⚠ 網走は z16 の一部だけ 404）
```

⚠ **明治期の低湿地とは違う。**⚠ **あちらは 404 なら「その資料が無い」だった**（`docs/adr/0056`）。

## 決めたこと ── ⚠ **地形分類には ③ が無い**

```text
⚠ **地形分類は、⚠ どこでも答えが出る。**
⚠ **詳細版が無い土地では、⚠ 広い区分に落ちる。**⚠ **落ちるだけで、⚠ 無くならない。**
```

⚠ **`public/verify.js` が、⚠ 既にこの構造を持っていた**（⚠ 2026-08-29 に確認）:

```text
z14〜16 が詳細版（主要平野部のみ）、z5〜13 が広域版・地域版（全国）。
詳細版が無いところで広域版に落ちるが、粗くなったことは必ず言う。
⚠ **「詳細版が無い」と「分類が無い」は別のこと。**
```

⚠ **`public/words.js` も、⚠ 詳細版 22 区分と広い区分 14 区分を分けて持っている。**
⚠ **こちらが測って出した結論は、⚠ β 版が既に持っていたものと同じだった。**

## ⚠ だから、⚠ まだ共通化しない

⚠ **`tmp/tmp2.md` の判断に従う**（2026-08-29。Owner）:

```text
⚠ 地形分類にも同じ構造がある   → ⚠ 共通のデータ状態モデルにする値打ちがある
⚠ **地形分類には ③ が無い**    → ⚠ **まだ共通化しない**   ⚠ ← こちら
```

⚠ **`AVAILABLE / UNCLASSIFIED / OUT_OF_COVERAGE / ERROR` のような
⚠ 共通モデルには、⚠ いまはしない。**

⚠ **出典ごとに、⚠ 「無い」の意味が違うから。**

| 出典 | ⚠ 取れないとき、⚠ それは何を意味するか |
|---|---|
| ⚠ 明治期の低湿地 | ⚠ **その資料が作られていない**（⚠ 代わりが無い） |
| ⚠ 地形分類 | ⚠ **詳細版が無いだけ。**⚠ **広い区分で答えられる**（⚠ 代わりがある） |

⚠ **同じ枠に入れると、⚠ この差が消える。**⚠ **消すと、⚠ 画面が「無い」と言い過ぎる**（掟 §1）。

## ⚠ 画面への効き方

```text
明治期の低湿地   ⚠ 3 状態（`docs/adr/0056`）
地形分類         ⚠ **2 状態**  ── ⚠ 詳細版で答えた ／ ⚠ **広い区分で答えた**
                 ⚠ **どちらでも答えは出る。**⚠ **粗くなったことは必ず言う**（`verify.js`）
```

⚠ **`docs/adr/0053` の基本形（⚠ 足元を言い切る）は、⚠ 地形分類では常に成立する。**
⚠ **「ここは○○」が出せない土地は、⚠ 測った範囲では無かった。**

## ⚠ 決めていないこと

```text
⚠ 「粗くなった」を、⚠ v0.1.0 の画面でどう言うか（⚠ β 版は判定カードで名乗っている）
⚠ 凡例が、⚠ 詳細版と広い区分で混ざる縮尺があるか（⚠ **測っていない**）
⚠ 空中写真・建物の状態（⚠ **まだ測らない。**⚠ `tmp/tmp2.md`: 早い）
```
