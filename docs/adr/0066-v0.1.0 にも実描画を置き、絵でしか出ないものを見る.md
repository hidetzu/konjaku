# 0066. v0.1.0 にも実描画を置き、絵でしか出ないものを見る

- 状態: 採用（⚠ **決めたのは Owner。**⚠ 2026-08-29）
- 日付: 2026-08-29

## 背景 ── ⚠ **1 日で 2 件、⚠ 静的検査が緑のまま通した**

```text
⚠ **写真の全面に、⚠ 前に塗った地形分類の青が乗っていた**
   ⚠ 描くのをやめただけで、⚠ canvas が前の絵を持ったままだった
⚠ **地理院タイルの出典が、⚠ 1 つも出ていなかった**
   ⚠ 地図を手で組んでいるので、⚠ MapLibre の帰属表示が付いてこない
```

⚠ **どちらも字を見る検査では見つからない。**⚠ **絵か、⚠ 実際の寸法でしか出ない。**
⚠ **`CLAUDE.md` §2 が言っているとおり**: ⚠ **「`npm run check` だけでは足りない。
実際に不具合を捕まえているのは実描画のほうが多い」。**

## 決めたこと

⚠ **`next` を suite として足す**（⚠ β の `top` / `peel` と並べる）。

```text
⚠ **別のサーバに立てる**   ⚠ v0.1.0 は別の Worker。⚠ **本番と同じく根が `/`**
                           ⚠ **同じサーバの下の階に置かない**（⚠ URL の形が変わる）
⚠ **狭い幅で回す**         ⚠ 375×667。⚠ **v0.1.0 は散歩中のスマホが相手**
⚠ **回す先の判定は 1 か所** ⚠ `test/render-scope.mjs` が `public-next/` → `next` と決める
```

## ⚠ 置いたケース（⚠ 4 件）

| 何を守るか | ⚠ なぜ |
|---|---|
| ⚠ **写真を出したら、地形分類の色が残っていない** | ⚠ **実際に踏んだ**（⚠ 画素を数える） |
| ⚠ **出典が見えていて、板にも一覧にも覆われない** | ⚠ **実際に踏んだ**（⚠ `elementFromPoint` で当てる） |
| ⚠ **年代が全部画面に収まり、44×44 を割らない** | ⚠ 利用者役 1 名が「7 個目に気づかなかった」 |
| ⚠ **保存すると一覧に残り、押すとその地点へ戻る** | ⚠ β は 18 日間 0 件だった（`docs/adr/0047`） |

⚠ **4 件とも、⚠ わざと壊して落ちることを確かめた**（⚠ 元の不具合と同じ形にして）。

## ⚠ 置いた瞬間に、⚠ 1 件見つかった

⚠ **`clearRect` したあとに、⚠ 先に走っていた塗りが返ってきて、⚠ 写真の上へ 805394 画素を描いた。**

```text
⚠ **手元では出なかった。**⚠ **取得が終わってから押していたから。**
⚠ **世代印（drawSeq）では防げない。**⚠ **世代は同じで、⚠ 変わったのは「写真を出しているか」のほう。**
```

⚠ **直し方**: ⚠ **`await` から返るたびに、⚠ いま写真を出していないかを見る**
（`change-review` §4「古い結果で、⚠ いまの画面を上書きしない」）。

## ⚠ 検査が主張を守っていなかった（⚠ 一度やり直した）

⚠ **はじめ、⚠ 幅を指定せずに置いた**（⚠ 既定の 1200px）。
⚠ **年代を横に流す形に戻しても、⚠ 落ちなかった。**⚠ **幅があるので 7 つとも収まる。**

```text
⚠ **守りたい主張は「375px で収まる」だった。**
⚠ **1200px で測っても、⚠ その主張は 1 度も確かめていない。**
```

⚠ **`CLAUDE.md` §9 の「わざと壊しても素通りした」と同じ形。**
⚠ **わざと壊す手順が無ければ、⚠ 気づけなかった。**

## ⚠ 静的検査と実描画で、⚠ 違うことを言う

| | 言えること |
|---|---|
| `test/check/next.mjs` | ⚠ **配信物に、⚠ 出典が書かれている** |
| `test/render/next.mjs` | ⚠ **画面に、⚠ 出典が見えていて、⚠ 覆われていない** |

⚠ **どちらも要る。**⚠ **静的検査は、⚠ 実描画が回らない変更でも落ちる**（⚠ 文書だけを触ったときなど）。

## ⚠ 取り違えを防ぐ

⚠ **β 側は `public/sw.js` の版で「自分が立てたサーバか」を見ている。**
⚠ **`public-next/` に `sw.js` は無い**ので、⚠ **配られた `index.html` を手元と 1 バイトずつ突き合わせる。**
⚠ **別のワークツリーがポートを取っていたら、⚠ 相手の画面を黙って測ることになる。**

## ⚠ 4 件足した（⚠ 2026-08-29。⚠ 同じ日のうちに）

⚠ **上の 4 件は「実際に踏んだ」ものを的にしている。**
⚠ **足した 4 件は「まだ踏んでいないが、⚠ 踏むと痛い」ところ。**
⚠ **観点は `ui-ux-review` §3 から借りた。**⚠ **新しい下限を発明していない。**

| 何を守るか | ⚠ 何で測るか |
|---|---|
| ⚠ **暗い色みでも、答えと出典が読める** | ⚠ 実際の計算値から明暗比（⚠ **4.5 は WCAG AA。⚠ こちらで決めた値ではない**） |
| ⚠ **320px でも、年代は折り返して収まる** | ⚠ 段数・44×44・横あふれ |
| ⚠ **資料が無い地域で、「無い」と「作られていない」を言い分ける** | ⚠ 松江（⚠ 明治期の資料が作られていない地域） |
| ⚠ **町名が取れなくても、保存は止まらない** | ⚠ **逆ジオの口を塞いで確かめる** |

⚠ **4 件とも、⚠ わざと壊して落ちることを確かめた。**

### ⚠ 気をつけたこと

```text
⚠ **暗い色みは、⚠ 先に「本当に暗いか」を確かめてから測る。**
   ⚠ **これが無いと、⚠ 明るいまま測って「通った」ことになる。**

⚠ **320px は「2 段になること」まで見る。**
   ⚠ **段数を見ないと、⚠ 幅の指定が効いていなくても通る**（⚠ 375px と同じ形で緑になる）。

⚠ **「無い」と言っていないことも見る。**
   ⚠ **「作られていません」が在るだけでは足りない。**⚠ **別の行で「ありません」と言っていないか。**

⚠ **町名は塞いで確かめる。**⚠ **「起きにくいから」で通さない**（`change-review` §4）。
```

## ⚠ 決めていないこと

```text
⚠ **v0.1.0 のケースを、⚠ 何本かに分けて同時に回すか**（⚠ いま 8 件なので要らない）
⚠ **文字を大きくして壊れないか**（⚠ `ui-ux-review` §3 の 1 項目。⚠ まだ見ていない）
⚠ **Tab でフォーカスが行くか・目で分かるか**（⚠ 同上）
```
