# 0067. いまの場所は URL で渡し、年代は渡さない

- 状態: 採用（⚠ **決めたのは Owner。**⚠ 2026-08-29）
- 日付: 2026-08-29

⚠ **`docs/adr/0048` の「試す順」の 3（⚠ URL / QR で手渡す）の、⚠ いちばん小さいぶん。**
⚠ **`docs/adr/0048` は、⚠ **URL に載せるのは「要るものが何も無い・言えなくなることが 1 つも無い」**
⚠ と測ってある。**

## ⚠ 測ってから決めた

⚠ **実測（2026-08-29）**: ⚠ **共有は、⚠ まだ何も成立していなかった。**

```text
⚠ 地図を動かしても URL が変わらない   ⚠ 開いたときの ?ll= のまま
⚠ 年代を押しても URL が変わらない
⚠ 保存した場所は URL に入らない       ⚠ 端末の中だけ
⚠ 共有のボタンが無い                  ⚠ アドレス欄を手で写すしかない
```

⚠ **読む口も書く口も、⚠ `place-arg.js` に既にあった**（`readPlace` / `placeQuery`）。
⚠ **v0.1.0 が呼んでいなかっただけ。**⚠ **作り直していない**（`CLAUDE.md` §3）。

## 決めたこと

```text
⚠ **送るのは、⚠ いま見ている場所だけ。**
⚠ **年代は送らない**（⚠ Owner 判断: ⚠ 「年代を共有する必要はない」）。
⚠ **保存した場所を丸ごと渡すのは、⚠ まだやらない**（⚠ `docs/adr/0048` の 3 の残り）。
```

## ⚠ 的の置き場（⚠ 3 案を測って決めた）

| | 板（375px） | ⚠ 起きたこと |
|---|---|---|
| ⚠ **あ 見出しの行**（⚠ 保存の隣） | ⚠ **347px。⚠ 変わらない** | ⚠ **採った** |
| い 板の下 | 348px | ⚠ **「保存した場所を見る」が 2 行に折り返す** |
| う 地図の上 | 増えない | ⚠ **何を送るのかが読みにくい**（⚠ 保存の 3 案でも同じ理由で採らなかった） |

⚠ **利用者役 3 名中 2 名が、⚠ 保存のときに「何を保存するかが分かるか」を理由に挙げている。**
⚠ **送るときも同じ問い。**⚠ **「いまいる場所」の隣なら結びついて読める。**

## ⚠ 押したあとに、⚠ 必ず字で言う

```text
⚠ 端末の共有の口がある     → ⚠ それを使う
⚠ やめた（AbortError）      → ⚠ **何も言わない**（⚠ 失敗ではない）
⚠ 口が無い／落ちた         → ⚠ 写す
⚠ 写せた                   → ⚠ 「写しました」
⚠ どちらも使えない         → ⚠ 「写せません」
```

⚠ **`docs/adr/0026`（⚠ 押しても何も起きない導線）。**
⚠ **「やめた」と「壊れた」を混ぜない**（⚠ 掟 §4-1: ⚠ こちらの都合を、⚠ 相手の都合のように言わない）。

## ⚠ 途中で 2 つ直した

```text
⚠ **320px で「いまいる場所」が 1 文字ずつ縦に潰れた**
   ⚠ 押したあとの字が長く、⚠ 見出しの行に 3 つが入らなかった（⚠ 板 295 → 357px）
   → ⚠ **字を「送る」に短くし、⚠ 押せるものを先に通す**

⚠ **空の `q=` を配っていた**
   ⚠ `placeQuery` は必ず地名を足すが、⚠ v0.1.0 は地図の真ん中の地名を持っていない
   ⚠ **受け取った人には、⚠ 壊れた URL に見える**
   → ⚠ **落とすだけにした**（⚠ 組み立て直さない）
```

## ⚠ 検査が 1 度素通りした

⚠ **「開いたときの URL をそのまま返す」形に壊しても通った。**
⚠ **`ll=` が在ることしか見ていなかった。**

```text
⚠ **足りなかったもの**: ⚠ **地図を動かしたのに、⚠ 座標が変わっていないこと**
```

⚠ **開いたときの座標と違うことを足したら、⚠ 落ちるようになった。**
⚠ **これで 4 通りとも落ちる**（⚠ そのまま返す ／ 年代を足す ／ 空の指定 ／ 字が変わらない）。

⚠ **`CLAUDE.md` §9 の「わざと壊しても素通りした」と同じ形。**⚠ **この日 3 回目。**

## ⚠ 決めていないこと

```text
⚠ **保存した場所を丸ごと渡すか**（⚠ `docs/adr/0048` の 3 の残り。
   ⚠ **50 件が 2000 文字に収まると実測済み**: `scripts/handoff-size.mjs`）
⚠ **PC で深掘りする画面**（⚠ `docs/adr/0048` の 2。⚠ `docs/adr/0064` は「入れない」と書いてある）
```
