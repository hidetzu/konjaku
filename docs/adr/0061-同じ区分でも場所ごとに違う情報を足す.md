# 0061. 同じ区分でも、場所ごとに違う情報を足す

- 状態: 採用（⚠ **決めたのは Owner。**⚠ 2026-08-29）
- 日付: 2026-08-29

## 背景 ── ⚠ 5 秒テストの 2 回目で出た

⚠ **`docs/adr/0060` で「説明を主に、区分名を従に」と直した。**
⚠ **`Q1「5 秒で分かったか」は 3/3 が「分かった」に変わった**（⚠ 前回は A が「足りなかった」）。

⚠ **代わりに、⚠ 前回 1 名だけが言っていた問題が、⚠ 3/3 になった。**

```text
⚠ **豊洲・松江・浦安で、⚠ 太字が一字一句まったく同じ。**

A  ⚠ **決まった文を貼っているだけに見えて、⚠ 急に信用が下がった**
B  ⚠ **1 枚目で感じた信用が、⚠ 3 枚並べたところで下がった**
C  ⚠ **同じ文で片づけられると、⚠ かえって信用が下がった**
```

⚠ **説明を主にしたことで、⚠ この問題が大きくなった。**
⚠ **前は区分名が主で、⚠ 説明は添え物だった。**⚠ **いまは主なので、⚠ 繰り返しが目立つ。**

## ⚠ 直し方は 2 つあった

```text
イ  ⚠ **場所ごとに違う情報を足す**   ⚠ 「いつ」「周りとの関係」など
ロ  ⚠ 説明文を場所ごとに変える      ⚠ **`docs/adr/0054` の「例外を作らない」に触れる**
```

## 決めたこと ── ⚠ **イ**

```text
⚠ **`docs/adr/0054` は崩さない。**
⚠ **36 区分すべてが同じ形の説明を持つ、⚠ という決めごとは残す。**
⚠ **場所ごとに違う情報のほうを、⚠ 足す。**
```

⚠ **ロを採らない理由**:

```text
⚠ **GROUND_GLOSS は、⚠ 利用者役 4 名の実測から作られている**（2026-08-21）。
⚠ **36 / 36 の取りこぼし無しを、⚠ 検査が保っている。**
⚠ **場所ごとに変えると、⚠ 「説明がある場所」と「無い場所」の差が意味を持つ。**
⚠ **しかも、⚠ 場所ごとの文を書く根拠が、⚠ こちらには無い**（⚠ 推し量って書くことになる。掟 §1）。
```

## ⚠ 何を足せるか（⚠ 調べてある。`docs/adr/0060`）

```bash
node scripts/survey-when.mjs
```

| 出典 | ⚠ 場所ごとに違うか |
|---|---|
| ⚠ 地形分類 | ⚠ **違わない**（⚠ 区分が同じなら同じ）。⚠ **「いつ」も持っていない** |
| ⚠ 明治期の低湿地 | ⚠ **違う**（⚠ 点ごとに区分が変わる）。⚠ 「明治期」という幅は言える |
| ⚠ 空中写真 | ⚠ **違う**（⚠ どの年代が在るかが土地で変わる） |
| ⚠ 点（Wikidata・伝承碑） | ⚠ **違う。**⚠ **点ごとの年を持つ** |
| ⚠ 面の内訳（⚠ viewport） | ⚠ **違う**（⚠ 豊洲は一色、⚠ 松江は 4 位 ── `docs/adr/0055`） |

⚠ **いちばん近いところに在るのは、⚠ 明治期の低湿地。**
⚠ **`verify.js` も `swale.js` も、⚠ もう運んである**（`docs/adr/0060` の縦切り）。
⚠ **画面に出していないだけ。**

## ⚠ 「いつ」は、⚠ 3/3 が気にしている

```text
A  ⚠ 「その後」がいつか（⚠ 5 秒の問いへの答えの中で、⚠ 自発的に言及）
B  ⚠ **江戸の埋立てと 10 年前の埋立てでは全然違う**
C  ⚠ **いちばん知りたいところなのに書いていない**
```

⚠ **`docs/adr/0060` は「地形分類には『いつ』が無いので言わない」と決めた。**
⚠ **それは変えない。**⚠ **代わりに、⚠ 「いつ」を持つ出典を足す。**

## ⚠ 「押したくならない」も、⚠ 同じ筋

⚠ **「ほか n 種」は、⚠ 3/3 が「押せそうに見える」に変わった**（⚠ 前回は 3/3 が「見えない」）。
⚠ **ただし 3/3 が「押したくならない」。**⚠ **理由も一致している**:

```text
⚠ **「何の 3 種か分からないので、⚠ 押した先が読めない」**
```

⚠ **`docs/adr/0026` は「押しても何も起きない導線」を禁じている。**
⚠ **「押す理由が分からない導線」も、⚠ 同じところに置く。**

## ⚠ 問いが 1 つ足りていなかった（⚠ 2026-08-29。Owner）

⚠ **利用者テストで見えてきたのは、⚠ これ**:

```text
⚠ **「ここは昔どんな土地？」だけではなく、
⚠ 「ここは昔何だった？ ⚠ **それはいつ頃？**」を期待している。**
```

⚠ **`tmp/next-konjaku.md` の最小体験は「場所の過去を数秒で知り」だった。**
⚠ **「いつ」が入っていなかった。**⚠ **こちらもそう読んでいた。**

⚠ **`docs/rules/domain.md` の 3 つの問いにも入っていない**:

```text
1  どの場所を見るか
2  その場所は昔と今でどう変わったか   ⚠ ← 「いつ」が要る、とは書いていない
3  選んだ場所を深掘りすると何が分かるか
```

### ⚠ これが効く先

⚠ **`docs/adr/0060` は「地形分類には『いつ』が無いので言わない」と決めた。**
⚠ **その決めごとは変えない。**⚠ **ただし、⚠ 「言わなくてよい」ではなくなった。**

```text
⚠ **地形分類だけでは、⚠ 利用者が期待する問いに答えられない。**
⚠ **「いつ」を持つ出典を足すことが、⚠ 好みではなく **要件**になった。**
```

⚠ **足せる出典は調べてある**（`docs/adr/0060` の表）:

```text
明治期の低湿地   ⚠ 「明治期」という幅は言える
空中写真         ⚠ 撮影年代を言える
点              ⚠ **点ごとの年を言える**
```

⚠ **3 名とも「いつ」を求めたが、⚠ 求めている粒度は違う**:

```text
A  ⚠ 江戸？戦前？昭和？          ⚠ **時代の幅で足りる**
B  ⚠ 江戸の埋立てと 10 年前では違う  ⚠ **幅で足りる**
C  ⚠ **いつまで水で、⚠ いつ埋めたのか**  ⚠ **年が要る**
```

⚠ **A・B には「明治期は…」で届く可能性がある。**⚠ **C には届かない。**⚠ **測っていない。**


## ⚠ 決めていないこと

```text
⚠ ~~次の縦切りでどの出典から足すか~~ → ⚠ **明治期の低湿地を足した**（⚠ この PR）
⚠ 「いつ」の粒度を、⚠ どこまで求めるか（⚠ 幅で足りるか、⚠ 年が要るか。⚠ **測っていない**）
⚠ `tmp/next-konjaku.md` の最小体験に、⚠ 「いつ」を書き足すか
⚠ 「いつ」を、⚠ 画面のどこで言うか
⚠ 「ほか n 種」の字を、⚠ 何に変えるか（⚠ 押す理由が読める字にする）
```
