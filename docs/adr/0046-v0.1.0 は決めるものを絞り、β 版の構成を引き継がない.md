# 0046. v0.1.0 は決めるものを絞り、β 版の構成を引き継がない

- 状態: 採用（⚠ **決めたのは Owner。**⚠ 2026-08-28）
- 日付: 2026-08-28

⚠ **これは「何を決めたか」ではなく、⚠ **「何をまだ決めないか」**の記録。**
⚠ **後のセッションが黙って決めてしまわないために残す。**

## 背景

⚠ **v0.1.0 は、⚠ β 版の画面・機能・データ構成を前提にしない**（`tmp/next-konjaku.md`）。
⚠ **最小体験は 4 段**（`docs/adr/0045`）。

⚠ **β 版を Evidence として測った**（⚠ 2026-08-28・`main` = `1e479ad`・375×667・
明るい色み・豊洲 35.6553,139.7967・Chromium）。⚠ **3 つの問いを Owner に出した。**
⚠ **3 つとも「まだ決めない」だった。**

## 決めたこと ── ⚠ **3 つとも、まだ決めない**

### ⚠ ① 入口は現在地か

⚠ **決めない。**⚠ **最有力仮説として試す。**

⚠ **実測**: ⚠ 何も指定せずに開くと、⚠ **出るのは検索窓**と「たとえば 豊洲・渋谷・広島」。
⚠ **足元の答えは 1 つも出ていない。**⚠ 現在地の的（📍）は ⚠ **37×30px**
（⚠ **規則の下限 44×44 を割っている**）。

⚠ **Owner が挙げた懸念**:

```text
⚠ 起動直後に現在地の許可を求めること自体が、
⚠ 散歩の体験を悪くする可能性がある。
```

⚠ **これは仮説を否定する材料ではなく、⚠ **仮説の中に隠れていた別の判断**。**
⚠ **「現在地を入口にする」と「起動直後に許可を求める」は、⚠ 同じことではない。**

| ⚠ 分けて考えること | 問い |
|---|---|
| ⚠ **何を最初に見せるか** | ⚠ 足元の答えか、⚠ 場所を選ぶ手段か |
| ⚠ **いつ位置情報を求めるか** | ⚠ 開いた瞬間か、⚠ 利用者が求めてからか |

⚠ **試す案**（⚠ どれも作っていない）: ⚠ A 開いた瞬間に求める ／ ⚠ B 押したときに求める ／
⚠ C 求めず、地図を動かして決める。

⚠ **許可ダイアログは静止画では出ない。**⚠ **絵で聞ける範囲を超えている**
（⚠ いままでの `visual-decision` では届かない）。

### ⚠ ② 画面をいくつに分けるか

⚠ **決めない。**⚠ **β 版の 2 画面（トップ / `/peel`）を守らない。**
⚠ **「知る → 残す → 掘る」から再設計し、⚠ 結果として 1 画面でも 2 画面でもよい。**

⚠ **実測**:

```text
場所を選んだあとのトップ   1684px ＝ ⚠ **2.5 画面**  ／ 見えている塊 21 個
  同じ主張のくり返し       ⚠ 「水だった」5 か所 ／ 年の数字 8 か所
/peel                     667px ＝ 1 画面  ／ 押せるもの 13 個
```

⚠ **「段階的に深掘りする」の逆で、⚠ 最初に全部出している。**
⚠ **ただし「だから 1 画面にすべき」は決めない。**⚠ **体験から決める。**

### ⚠ ③ 4 段目（別の端末で続きから）の方式

⚠ **決めない。**⚠ **先に「別端末で続けられること自体に、⚠ どれだけ価値があるか」を確かめる。**

⚠ **方式の案と、⚠ それぞれが何を要求するかは `docs/adr/0045`。**⚠ ここには書かない。

## ⚠ ④ 「保存まで行かない」という仮説（⚠ 2026-08-28 に Owner が立てた）

⚠ **Owner の言葉**:

```text
⚠ 今の β 版だと、⚠ 保存するというところの前に情報がごちゃっとしていて、
⚠ 興味を持って保存する流れにいかないのではないか。
```

⚠ **これは仮説。**⚠ **決定ではない。**⚠ **確かめていない。**

### ⚠ 測ったこと（⚠ 観測）

⚠ **実測 2026-08-28・`main` = `1e479ad`・375×667・明るい色み・豊洲（35.6553,139.7967）・Chromium**

```text
スクロールできる幅        1017px
☆ が画面に在るのは        0〜85px      ⚠ **8%**
☆ が画面に無いのは        90〜1017px   ⚠ **92%**

☆ の名乗り                字「☆」／ title「この場所の評価とメモ」／ ⚠ aria-label 無し
「保存」と読める的        ⚠ **0 個**（⚠ 画面のどこにも「保存」「残す」と書かれた的が無い）
```

⚠ **興味が湧く情報の位置**:

```text
y  118   この土地は 旧水部
y  849   明治期（1868〜1912年）は、ほとんど 河川・湖沼・海面 100.0%
y  990   標高 -1.84m（海面より低い）
y 1063   ⚠ この土地で気をつけること
```

⚠ **ごちゃつきの側も測れている**:

```text
場所を選んだあと          1684px ＝ 2.5 画面 ／ 見えている塊 21 個
同じ主張のくり返し        ⚠ 「水だった」5 か所 ／ 年の数字 8 か所
```

### ⚠ 解釈（⚠ 観測と分ける）

⚠ **仮説は支持されるが、⚠ 形が違う。**

```text
⚠ 「情報がごちゃっとしていて、⚠ その先の保存に行かない」ではなく——
⚠ **保存の的は情報より前に在り、⚠ 85px スクロールした時点で消える。**
⚠ **一番「残したい」と思う瞬間に、⚠ 的が存在しない。**
```

⚠ **順番が逆になっている。**⚠ **加えて `☆` は保存だと名乗っていない**
（⚠ `title` は「この場所の評価とメモ」。⚠ **「あとで戻れるように残す」ものだと字から分からない**）。

⚠ **「ごちゃっとしている」は事実**（⚠ 2.5 画面・21 塊・同じ主張が 5 か所）。
⚠ **ただし「だから保存に行かない」は、⚠ この測定からは言えない。**
⚠ **言えるのは「保存の的が 92% の位置で存在しない」まで。**

### ⚠ 反証になりうること

⚠ **興味そのものは湧いている可能性がある。**

```text
judged.ok  341  →  open.peel  219    ⚠ **64% が深掘りへ進んでいる**
```

⚠ **出典は hidetzu/konjaku#359 の表**（⚠ こちらで D1 を読み直していない）。
⚠ **深掘りまで行く人は 3 人に 2 人。**⚠ **保存だけが観測されていない。**

⚠ **つまり「興味を持たないから保存しない」ではなく、
⚠ 「興味は持つが、⚠ その時点で保存できない」かもしれない。**
⚠ **どちらかは、⚠ まだ確かめていない。**

### ⚠ この仮説から出てくる、まだ決めていないこと

```text
⚠ 保存の的を、⚠ 情報の「あと」に置くか ／ ⚠ ついてくる形にするか
⚠ ☆ を、⚠ 「保存」と名乗らせるか（⚠ いまは「評価とメモ」）
⚠ 2.5 画面・21 塊を減らすか（⚠ v0.1.0 は β 版を前提にしないので、⚠ 作り直す側の話）
```

⚠ **どれも決めない。**⚠ **v0.1.0 を「知る → 残す → 掘る」から組み直すときの材料にする。**

## ⚠ ③ を確かめる材料が、いま無い

⚠ **これは実測だが、⚠ **こちらが D1 を読んだのではない**。**
⚠ **hidetzu/konjaku#359 に載っている表を引いた**（⚠ そこに「2026-08-28 に D1 `konjaku` を読んだ・
⚠ 2026-08-11 〜 08-27」と書いてある）。⚠ **`shared` / `saved` の 0 件は `worker.js` のコメントが出典。**
⚠ **こちらで読み直していないので、⚠ 「確認済み」とは書かない**（`CLAUDE.md` §1）。

```text
judged.ok       341 件    ⚠ 判定が出た
open.peel       219 件    ⚠ 深掘りへ進んだ
era.moved       121 件    ⚠ 年代を動かした
open.speak       18 件
judged.coarse     3 件
shared / saved    0 件    ⚠ 17 日間ずっと 0
```

⚠ **`saved` は「画像として保存した」であって、⚠ **場所を残した**ではない**（`worker.js` の一覧）。

⚠ **場所を残したことを数える event は、⚠ 1 つも無い。**
⚠ **★とメモは `localStorage` にしか無く、⚠ こちらへは届かない**（⚠ 画面がそう約束している）。

⚠ **つまり**:

```text
④ の価値を確かめたいが、⚠ **④ が乗っている ② が使われているかすら分からない。**
```

⚠ **推し量って埋めない**（`CLAUDE.md` §1）。⚠ **「使われていない」とも書かない。**
⚠ **観測していないだけ。**

## ⚠ 先にできること

⚠ **② が使われているかを数える。**⚠ **これは既存の約束の中で足せる。**

```text
足すもの   ⚠ 「場所を残した」を 1 件数える event
持たない   ⚠ どこを残したか ／ 誰が残したか ／ いくつ持っているか
粒度       ⚠ 日ごとの回数だけ（⚠ 既存の tick と同じ形）
```

⚠ **これで分かるのは「残す人がいるか」まで。**
⚠ **「別の端末で続けたいか」は、⚠ これでも分からない。**⚠ **そこは聞くしかない。**

⚠ **足すかどうかは決めていない**（⚠ v0.1.0 は β 版を前提にしないので、
⚠ **β 版に足す意味があるかから決まる**）。

## ⚠ 決めていないこと（⚠ まとめ）

```text
① 入口を現在地にするか ／ いつ位置情報を求めるか ／ どう試すか
② 画面をいくつに分けるか
③ 4 段目の方式 ／ ④ の価値をどう確かめるか
   ⚠ β 版に「残した」を数える event を足すか
④ 保存の的をどこに置くか ／ ⚠ 「保存」と名乗らせるか ／ ⚠ 情報の量を減らすか
```
