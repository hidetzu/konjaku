# 0078. プライバシーは先に 4 点を渡し、くわしくは下に置く

- 状態: 決めた
- 日付: 2026-09-01

## 何を決めたか

⚠ **`/privacy` を、⚠ 法務文書ではなく「30 秒で分かるもの」にする。**

```text
上   4 点だけ（平易な言葉）
下   くわしく（1〜4 の見出しが、上の 4 点と 1 対 1 で対応する）
```

⚠ **Owner の言葉（2026-09-01）**:

> 冒頭に「端末内保存」「別端末共有時のみ一時送信」「行動追跡はしない」
> 「地図・住所検索では外部サービスへ位置情報を送る」の 4 点を平易な言葉で要約し、
> その下に詳細説明を置いてください。
> 実装用語や硬い表現（ハッシュ、保管庫、削除ジョブ等）は避け、必要なら詳細側で補足してください。
> 現在の「ここに書いていないことは、していません」という姿勢は残してください。

## ⚠ 事実は減らしていない

⚠ **これがいちばん大事なところ**（`CLAUDE.md` §1）。⚠ **変えたのは順番と言葉だけ。**

⚠ **前後の本文を文で切って、⚠ 機械的に突き合わせた**（⚠ 25 文 → 36 文）。
⚠ **形が変わった 7 文は、⚠ すべて対応する文が後にある。**

```text
保管庫に置かれます            →  こちらで預かります
合言葉から作った、             →  合っているかを確かめるための別の字に変えて持ちます。
元に戻せない字だけを持ちます       その字から、元の合言葉は分かりません
通信のこと（見出し）           →  4. 地図と住所の検索は、国土地理院へ問い合わせます
数えていないこと（見出し）       →  3. 誰がどこを見たかは、数えていません
```

⚠ **1 文だけ、⚠ 本当に落ちかけた**: ⚠ **「調べた場所と保存した場所を、どう扱っているかを書きます」。**
⚠ **これは文書の範囲を言う文で、⚠ 「ここに書いていないことは、していません」の土台。**
⚠ **突き合わせで見つけて、⚠ 冒頭へ戻した。**

## ⚠ とくに消してはいけないもの

⚠ **やさしくすると、⚠ 言えないことのほうが先に消える。**⚠ **検査が名指しで見ている。**

```text
期限を過ぎたものをまとめて消す仕組みは、まだ置いていない
だから「消えます」とは書かない
接続元の IP と端末の種類は、配信元に届く
預けるもの（緯度・経度・町名・足元の区分・保存した時刻）
合言葉そのものは預からない
```

⚠ **「期限が来ると使えなくなる」と「消える」は別。**⚠ **後者は、いま言えない。**

## ⚠ 言葉で気をつけたこと

- ⚠ **要約の ② は「こちらのサーバへ送るのは」と書く。**⚠ **「こちらへ送るのは」だと、
  ⚠ ④（国土地理院へ届く）と読み合わせたときに矛盾して見える。**⚠ **送り先を分ける。**
- ⚠ **④ に「これは避けられません」を足した。**⚠ **地図を出す以上、⚠ 選べない**ことを言う。
  ⚠ **できないことから書き始めない**（§4-1）ので、⚠ 何ができるかを先に言った上で添える。
- ⚠ **番号を振った**（1〜4）。⚠ **上の 4 点との対応が、⚠ 目で追える。**
  ⚠ **番号が意味を持つので消さない**（`about.css` の条文と同じ扱い）。

## これを守っているもの

⚠ **`test/check/next.mjs` ⑲。**⚠ **6 通りわざと壊して、⚠ 6 通りとも落ちることを確かめた。**

- 姿勢の一文が在る
- ⚠ **要約が、⚠ くわしい説明より前に在る**（⚠ 順番そのものを見る）
- 要約が 4 点で、⚠ **4 つの主題に 1 つずつ触れている**（⚠ 字は写さない）
- 硬い言い方（ハッシュ・保管庫・削除ジョブ・当社・本規約・甲・乙）が無い
- ⚠ **上に挙げた「消してはいけないもの」が全部残っている**

## ⚠ 実測（2026-09-01）

```text
375×667   姿勢 あり ／ 硬い語 0 ／ 横あふれ 無し ／ 要約の下端 y=666
1280×800  姿勢 あり ／ 硬い語 0 ／ 横あふれ 無し ／ 要約の下端 y=490
```

⚠ **375×667 では、⚠ 見出しと 4 点がほぼ 1 画面目に収まる。**
