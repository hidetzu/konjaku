# 今昔（konjaku）— 作業のきまり

このファイルは**どう作業するか**を書く。
**何が言えるか**は `docs/SPEC.md`、判断の経緯は `docs/adr/` にある。
⚠ **どう書くか**（Domain の分け方・JavaScript・CSS・検査の優先順位・git の禁止操作）は
`.claude/rules/` にある（2026-08-22 に足した）。
再現可能な実測は検査コードとその出力を正とする。
重複させない。仕様が変わったら SPEC を直す。ここには書かない。

作業を始める前に、`docs/SPEC.md` の「掟」の節と、⚠ **`.claude/rules/README.md`** を読む。

---

## 1. いちばん上の原則

> **面白さより先に、情報が正しいこと。**

その上で、利用者の知的好奇心・探求心をくすぐる。順序を入れ替えない。

この3つは、コードでも画面でも崩さない。

```
観測されていない  ≠  存在しなかった
取得できなかった  ≠  存在しなかった
データにない      ≠  現実にない
```

だから、

- **確認できないことを「検査済み」と呼ばない**
- **取得できなかったことを「無い」と言わない**
- **推定を実測のように見せない**

「テストが通った」ではなく、**そのテストは本当にその主張を検証しているか**を見る。
過去に「検査が測っていないことを『確認済み』と表示していた」問題が出ている。

---

## 2. 検査

⚠ **回し方は `.claude/skills/verify/SKILL.md` にある。**ここには書かない
（重複させない。⚠ **2 か所に書くと、片方だけ古くなる**）。

`verify` が持つもの: 3 つのコマンドと部分実行の口 ／ Inner Loop と Final Gate ／
⚠ **落ちたのがこちらの不具合か外部かの切り分け** ／ 外部リンクの 2 段 ／
⚠ **fork では走らない検査** ／ `PASS` `FAIL` `NOT-VERIFIED`。

ここで言うのは 3 つだけ。

- ⚠ **`npm run check` だけでは足りない。** 実際に不具合を捕まえているのは実描画のほうが多い
- ⚠ **直した不具合は、検査として残す。** 同じことがもう一度起きたら止まる形にする。
  ⚠ **足したら、わざと壊して、本当に落ちること**を確かめる
- ⚠ **CI が緑でも「仕様が正しい」ではない。** とくに fork からの PR は
  実描画・検索・外部リンクが走っていない

---

## 3. アーキテクチャの境界

```
Runtime 依存を増やさない
静的配信できるものは静的にする
推論は事前処理へ寄せる
ブラウザは表示に集中
```

理由なく導入しない:
React 等への全面移行 / SPA フレームワーク / 状態管理ライブラリ /
Runtime DB 依存 / Worker API の大量追加 / Wikidata の実行時クエリ拡大 /
Overpass の実行時依存拡大 / 大規模な 3D エンジン交換。

今後やるべきは**新しい仕組みを増やすこと**より、
**同じ意味を持つ処理を一つにまとめること**。

⚠ 同じ問いに答える実装を2つ持たない。やむを得ず持つときは、機械で突き合わせる。
取り込み側とブラウザ側の両方に同じ判断を書くと、**取り込み済みの土地と
実行時に問い合わせる土地で答えが変わる**（precision で一度やっている）。

---

## 4. 言葉

- **内部状態をそのまま画面に出さない。** `NO_RECORD` `UNKNOWN` ではなく、
  「この年代に表示できる記録はありません」「この場所は資料の対象範囲外です」
  「現在、資料を確認できません」のように、人が読める言葉にする
- **機能名は、内部構造ではなく利用者の問いから決める。**
  `Historical View` `3D Mode` `Evidence` ではなく、
  「昔を見る」「この場所を深掘り」「なぜそう言える？」
  （⚠ 2026-08-18 に「立体で見る」→「この場所を深掘り」。見せ方の名前より、
  **その場所について何をするか**の名前にした）
- **一括置換しない。** UI の文言を変えるとき、設計記録・ADR・過去の議論まで
  機械的に置換しない。対象は**いまの利用者向け UI** を優先する

### 4-1. できないことから書き始めない

⚠ **隠すのではない。** §1 が上位で、**限界は必ず書く**。
変えるのは**順番・主語・時制**であって、書くか書かないかではない。

- **先に「ここで何ができるか」を言う。** できないことは、**理由**と
  **代わりにできること**を添えて、そのあとに言う
- **進行形を、状態の説明に使わない。** 「取得中」「届いていない」は、
  いま自分の端末で起きていることに読める
- **こちらの都合を、相手や回線の都合のように言わない。**
  こちらが用意していないことを「取得できませんでした」と言わない。
  ⚠ 利用者にとっては**押し直すべきか・待つべきか・諦めるべきか**が変わる
- **`⚠` を、対応状況や在庫に使わない。** `⚠` は危険（災害リスク）に使っている。
  同じ印を在庫の話に出すと「危ない土地の警告」に読まれる
- **止まって聞こえる言い方をしない。** 「用意できた場所から」より
  「**対応した場所から順に増やしています**」。また来る理由を残す

実際に直した例（2026-08-18。⚠ 利用者役 3 名に画面だけを見せて確かめた。実在の利用者ではない）。

| やめた | かわりに |
|---|---|
| `⚠ この場所の建物データはまだ用意できていません` | `空中写真を年代で切りかえて、明治期の地面と見くらべる（建物ごとの判定は、対応した場所から順に増やしています）` |
| `建物データを取得中` ／ `まだ届いていないだけで` | `建物ごとの判定は、この場所ではまだ提供していません` |
| `取得できませんでした（Overpass 混雑）` | `通信の問題ではありません。対応した場所から順に増やしています` |

⚠ **左の言い方は、3/3 が「自分の通信のせい」と読んだ。** 2 名は「押し直す」と答えた。
⚠ 一方で「**現地に建物が無いという意味でもありません**」は**消さない**。
やわらげた結果、「無い」と読まれては §1 に反する。

---

## 5. コメント

このリポジトリはコメントが多い。「なぜこの処理か」「なぜこの値か」「何を避けているか」が
残っている。これは資産だが、**古くなったコメントはコードより強く誤誘導する**。

コードを変えたら、次をセットで更新する。

```
実装 → 検査 → コメント → README → docs/SPEC.md
```

⚠ 検査が文書やコメントを読むとき、**コメントを先に落とす**。
落とさないと、検査を説明するコメントに書いた字面を検査自身が拾う（2回踏んでいる）。

---

## 6. 数字の書き方

- **必ず主張範囲の分母で書く。** 別の範囲の数字を持ち込むと、その数字自体が嘘になる
- 実測でない数字を書かない。確率・確信度は出さない
- 実測値を残すときは、**いつ・どこで・どうやって測ったか**を再現できる形にする
- 全国の割合を、その場の一覧（8行）の下に書かない。分母が違う
- ⚠ **外向けの文章（記事・OGP・SNS・README）にも、同じ規則を適用する。**
  数字には**主張範囲の分母・測定日・測定条件**を添え、`docs/SPEC.md` §3 と
  検査結果に照合する。**同じ主張を画面と外向けで書くときは、同じ分母を使う。**
  ⚠ 別の範囲を扱ってよい。ただし**どの範囲の数字かを明示する**
  （例: 豊洲 533件 と、配っている 403,397件 は別の主張）。
  実際に踏んでいる: README が「545件/542件(99.4%)」を掲げていたが、
  画面は同じ主張を 533件/99.6% で出していた。数え方が違うだけだが、
  **README と OGP は共有先まで届く**ので、そこだけこの掟の外に出る

---

## 7. 進め方

1. **磨くより先に測る。** 直す前に、いまどうなっているかを px と件数で出す
2. 観測（実測値・スクショ）と推測（解釈）を**必ず分けて**報告する
3. 検証していないことを「確認済み」と報告しない
4. `Non-goals` の外へ変更を広げない。目的を満たす最小範囲を基本とする

### 7-1. 自分で決める／人に聞く

⚠ **既定は「自分で決めて、最後まで進める」。** 手が止まるほうが損。
聞くのは、**間違えたときに取り返しがつかない**ものだけ。

| 自分で決める | 人に聞く |
|---|---|
| 実装の手順・切り出し方・検査の置き方 | ⚠ **画面に出る言葉**を変えるとき |
| 何をどう測るか | ⚠ **やることの範囲**が動くとき（`Non-goals` をまたぐ） |
| 直し方が 1 つに決まる不具合 | ⚠ **何が言えるか**（`docs/SPEC.md`）が変わるとき |
| 文書・コメント・検査を足す | 見せ方の案が 2 つ以上あって、**実測では決まらない**とき |

- 聞くときは **`AskUserQuestion`** を使う。⚠ **本文に混ぜて聞かない**（見落とされる）。
  これを使うと **Slack で聞いて、答えを受け取る**（`.claude/hooks/ask-slack.mjs`）。
  ⚠ **Slack へ出るのはこれだけ**。進捗も完了も失敗も送らない（埋もれさせない）
- ⚠ **測れば決まることを聞かない。** 先に測る（上の 1）
- ⚠ **聞くと止まる。** 答えに依存しない仕事は、聞く**前に**終わらせておく
- ⚠ **聞いたことは記録する。** 決まったら、その理由を `docs/adr/` か `tmp/` に残す

### 7-2. `ready-for-ai`（AI へ渡してよい Issue）

⚠ **これは優先度ではない。**「人が判断しなくても、AI が最後まで進められる」という印。

⚠ **付けるのは人だけ。**AI は付けない。判定を出すところまで
（`.claude/skills/issue-ready/SKILL.md`）。

⚠ **付けてよい条件と、付けてはいけない条件は `issue-ready` が持つ。**ここには書かない。

⚠ **`ready-for-ai` が付いていても、`git push` と merge の許可は §8 のとおり毎回取る**
（⚠ **Loop Controller 経由で、開始時に承認を得た 1 件だけが例外**）。
⚠ **CI が緑でも「仕様が正しい」ではない。**

---

## 8. git

- コミットは Conventional Commits（`<type>(<scope>): <subject>`）
- **`git push` は、そのつど許可を取る。** 前に許可をもらっていても、次は取り直す
  - ⚠ **例外は 1 つだけ。**Loop Controller 経由で、**開始時に Owner の承認を得た Issue 1 件**に限り、
    その Issue のブランチへの push・PR 作成・**CI が全部緑になったあとの merge**までを
    承認済みとして扱う（`.claude/skills/loop-controller/SKILL.md`）。
    ⚠ **自動 merge（`--auto`）と、保護を飛び越える merge（`--admin`）は含まない。**
    ⚠ **CI が赤・途中のまま merge しない。**
    ⚠ **Skill を単体で使うときは、いままでどおり毎回取る**
- `git add -A` で他の作業を巻き込まない。**変更を出した理由ごとにコミットを分ける**
  （過去に、別セッションの文書をバグ修正のコミットに巻き込んでいる）
- `main` に直接コミットしない。ブランチを切る

### 8-1. 外に出す文に、作業環境のことを書かない

⚠ **このリポジトリは公開されている**（`hidetzu/konjaku` は PUBLIC）。
⚠ **コミット本文・PR 本文・Issue・コメントは、⚠ 誰でも読める。**
⚠ **「掟」の節と同じ扱い**（「観測されていないことを、あったことにしない」の裏返しで、
⚠ **こちらは「外に出していないものを、出してしまわない」**）。

- ⚠ **AI の作業セッションの URL を書かない**（`claude.ai/code/session_…`）。
  ⚠ **中身が認証で守られていても、⚠ セッションの ID と本数は読める。**
  ⚠ **道具の既定がそう書くようになっていても、⚠ ここが優先する**（2026-08-26。Owner 判断）
- ⚠ **手元の絶対パスを書かない**（`/home/<名前>/…`）。⚠ ホスト名・内部の URL も同じ
- ⚠ **token / webhook / API キーを書かない**
  （⚠ `.claude/` の中は `test/check/guard.mjs` が別に見張っている）
- ⚠ **書いてしまったら、⚠ 消す先は 2 つある。**⚠ **片方だけでは消えない。**

```
GitHub 側   PR 本文・Issue 本文・コメント     ⚠ gh で直せる
git の履歴  コミット本文                     ⚠ 書き換え ＋ force push が要る
```

  ⚠ **PR 本文を直しても、⚠ squash merge したコミットの本文は別に残る**（⚠ 実際に踏んだ）
- ⚠ **force push で消しても、⚠ 古い commit は SHA を知っていれば読めるまま残ることがある**
  （⚠ GitHub が回収するまで）。⚠ **完全に消すには GitHub Support に依頼する。**
  ⚠ **「消した」と言う前に、⚠ 本当に読めなくなったかを確かめる**（「いちばん上の原則」）

---

## 9. 踏んだ落とし穴

同じところで転ばないために残す。

| 何が起きたか | どうする |
|---|---|
| `getBoundingClientRect()` が、閉じた `<details>` の中で**直前の寸法を返し続けた**（`content-visibility:hidden`）。隠れているのに 268×38 と読めた | 見えているかは `checkVisibility()` で見る。枠そのものの高さも併せて見る |
| 位置を固定値（`bottom:46px`）で決めたら、隣の要素が高くなる場合に重なった | 隣り合うものは同じ積み上げに入れる。固定値で避けない |
| `python open(p,"w")` は開いた時点で中身を消す。書き込み前に落ちるとファイルが 0 バイトになる | 書き込みの前に、必要なことを全部 assert してから開く |
| 取り込みが 1 区画だけ落ちたのに、ログを見ずに配った | 取り込みのあとはログを見る。`coverage.spec` が全区画で揃っていることは静的検査が見る |
| 静止画の経路だけ直して、地図の経路に届いていなかった（2回） | 同じ結果を出す経路が複数あるなら、**1 か所にまとめてから**直す |
| `_headers` は**一致するルールが全部連結される**（最も具体的なものが勝つのではない） | 実ファイルで確かめる |
| Playwright の glob で `(a|b)` は選択にならない（`{a,b}` が選択） | URL 述語で書く |
| `npm run render --group=core` は **npm が `--group=core` を飲む**ので、黙って**全群**が走る。落ちないので気づかない（数字だけ多い） | `npm run render -- --group=core` と `--` を挟む。⚠ 走った群は出力の1行目で名乗るので、そこを読む |
| ⚠ **CI の実描画が落ちても、まず自分の変更を疑わない。**ブラウザの用意（`apt`）が固まることがある。⚠ **実測（2026-08-19）は「上限に張り付く試行が約 40%」だったが、⚠ 2026-08-22 に測り直したら 20 / 20 で通っていた**（12〜35 秒・中央値 14 秒）。⚠ **相手側（apt のミラー）の調子は時期による** | 出力に「⚠ ブラウザの用意が N 回目で固まった」が出ていたら**相手側**。テストは 1 秒も走っていない。やり直す |
| ⚠ **検査が「相手先がいま何を返すか」を主張していた。**出島で、地理院が 1 回 404 を返さなかっただけで落ちた（実描画の失敗の半分） | ⚠ **こちらの正しさだけを主張する。**外の答えに依存する部分は、**実際に返ってきた値**を控えてから判定する |
| ⚠ **新しいファイルを `git add` する前に検査して、通ったと思った。**`git ls-files` を見る検査（Issue 番号の走査など）は、**追跡されていないファイルを一生見ない** | 新しいファイルを足したら、**`git add` してから** `npm run check` を回す |
| 文書に**裸の番号**で Issue を指して落ちた。⚠ 移行後は同じ番号が別の Issue を指す | ⚠ **リポジトリ名つきで書く**（`<owner>/<repo>#<番号>`）。名前つきなら移行しても同じものを指す。⚠ ここに実例の番号を書かない（検査が自分の字面を拾う） |
| ⚠ **検査は緑なのに、⚠ 画面が嘘をついていた。**実描画で地図の絵を差し替えたら、⚠ **軽井沢のバッジが「1年代」→「7年代」に化けた**（実在しない年代が「撮影されている」ことになった）。⚠ **その検査は年代数を主張していないので、⚠ 精読では気づけない** | ⚠ **判定の字（`✓` の行）を、⚠ 変更前後で 1 件ずつ突き合わせる。**⚠ **「全部緑」は、⚠ 何も変わっていないことの証明ではない** |
| ⚠ **静的検査そのものが、⚠ 間違った方向へ後押ししていた**（「アプリが読む年代 ⊆ 塞ぐ一覧」＝「全部塞げ」を見張っていた） | ⚠ **検査を足すときは、⚠ その主張が本当に正しいかを疑う。**⚠ 検査は「守るべきこと」を固定するので、⚠ **間違った主張は間違ったまま固定される** |
| ⚠ **足した検査が、⚠ わざと壊しても素通りした。**「分けて足すと元に戻る」だけを見ており、⚠ **全体からも同じだけ減ると気づけなかった** | ⚠ **突き合わせる相手は、⚠ 別の道で得たものにする**（走者に数えさせるなら、⚠ 一覧そのものとも突き合わせる） |
| ⚠ **手元で通り、⚠ CI で落ちた。**`--count` がブラウザを読み込んでおり、⚠ **静的検査のジョブには Playwright を入れていない** | ⚠ **手元に入っているものを、⚠ CI にもあると思わない。**⚠ 数えるだけの口は、⚠ **重いものを読み込まない** |
| 検査を足したのに `docs/SPEC.md` の件数を直さず、緑のまま `main` へ出た。⚠ **直すようにしたら、今度は並行作業で必ず競合した**（2026-08-22 に同じ日で 3 回。⚠ 競合中は CI が走らないので「CI が出ない」形で詰まる） | ⚠ **SPEC に件数を書かない**（2026-08-22）。⚠ **数は走者が出力する**（`check.mjs` は ✓ を数え、`render.mjs` はケース数を数え、⚠ その場で名乗る）。⚠ **書き戻すと静的検査が落とす** |
| ⚠ **足した検査が、⚠ 別の文を拾って素通りした**（2026-08-23。⚠ **静的と実描画で 1 回ずつ**）。「調べた場所が配信元へ届く」の文を**丸ごと消しても緑**だった。⚠ **「接続元の IP が配信元に届きます」という別の文が残っていて、それを拾っていた。**⚠ **IP が届くことと、調べた場所が届くことは別の主張** | ⚠ **文で切ってから見る**（`。` と改行）。⚠ **1 つの文の中で、主語と述語が結びついていることまで見る。**⚠ 本文全体に対する `test()` は、⚠ **どこかに似た語があれば通る** |
| ⚠ **打ち消しが効かず、⚠ 画面の主役が消えていた**（2026-08-23）。狭い幅の既定に `#panel.open ~ #hud{display:none}`（id2/class1）と書き、PC で `#panel ~ #hud{display:flex}`（id2/class0）で戻したつもりが**負けていた**。⚠ **実測（1280×950・豊洲）: `#hud` が 0×0。**⚠ **年代を一切動かせない**（⚠ `/peel` の主機能）。⚠ **同じ形の取り違えを、⚠ 補足でも踏んでいる**（2026-08-22。PC で 0×0） | ⚠ **打ち消しは、⚠ 同じ形のセレクタで書く**（詳細度をそろえる）。⚠ **この画面は「狭い幅が既定」**なので、⚠ **既定に書いたものは PC にも効く**（`.claude/rules/css.md`）。⚠ **消えたことに気づけるのは実描画だけ** |
| ⚠ **器の有無で待って、⚠ 手元で通り CI で落ちた**（2026-08-23）。`.prov-q` は建物が届く前から作られるので、⚠ **「器がある」を待つと、⚠ 速い環境では偶然中身も間に合う。**⚠ 手元 162/162 緑・CI 10 件失敗 | ⚠ **器ではなく、⚠ 落ち着いたこと（結果の字）を待つ。**⚠ 確かめ方は「遅らせる」。⚠ **建物タイルを 8 秒遅らせたら 529ms → 16623ms** になり、⚠ **待てていないことが数字で出た** |
| ⚠ **わざと壊した行を、⚠ 戻す前に `git add` して push していた**（2026-08-27。hidetzu/konjaku#277 の 40 本目）。⚠ **「壊す → 実描画 → 戻す」を 1 つのコマンドで回していて、⚠ 途中で `git add` が走った。**⚠ **作業ツリーは戻っていたので `git diff` では見えない。**⚠ **反転した主張は「作文の語が入っていること」を要求するが、⚠ 手元では偶然どれも入らず ✓ になり、⚠ CI で「このころ」が入って ✗ になった** | ⚠ **`git diff` ではなく `git show :<path>` で index を見る**（⚠ 「戻した: 0 行の差」は作業ツリーの話）。⚠ **わざと壊す前に `git add` しない。**⚠ **落ちてくれたのは運**で、⚠ **緑で通っていたら、⚠ その検査は永久に何も見なくなっていた** |
| ⚠ **同じ機械の別セッションと、⚠ 作業ツリーと 8099 番を取り合った**（2026-08-27）。⚠ **実描画が 181 件「接続拒否」で落ちた。**⚠ **相手は `git add -A` も使うので、⚠ こちらの未完成のファイルまで index に入りうる** | ⚠ **`git worktree` で切り離す**（⚠ `node_modules` は親から symlink で借りる）。⚠ **`KONJAKU_RENDER_PORT` をずらす**（⚠ 走者が落ちたときに自分でそう言う）。⚠ **`ERR_CONNECTION_REFUSED` は「こちらの主張が壊れた」ではない** |
