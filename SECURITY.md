# 脆弱性の報告

## 報告のしかた

GitHub の **Security Advisories** から、**非公開で**報告してください。

> リポジトリの **Security** タブ → **Report a vulnerability**

⚠ **公開の Issue には書かないでください。** 直る前に手口が広まります。

⚠ このプロジェクトは個人が作っているもので、**対応の期限を約束できません**。
受け取ったことの返事と、直したかどうかの連絡はします。

## 何を報告してほしいか

このサービスは**サーバー側の処理をほとんど持たない**（判定はすべてブラウザの中で動く）。
それでも次のようなものは報告してほしい。

- **外から来た文字列が、HTML やスクリプトとして実行される**
  （Wikidata のラベル・地理院の地名・OSM のタグなど。一度実際に起きている）
- 共有された URL を開くだけで、意図しないことが起きる
- 計測の受け口（`/t`）を通じて、こちらの資源を消耗させられる
- 利用者の端末に保存しているもの（★・メモ）を、別のサイトから読める
- ⚠ **「地名も座標も送らない」という約束が破れている**（下記）

## ⚠ プライバシーの約束が破れていたら、それも報告してほしい

この製品は次を掟にしている（`docs/SPEC.md`）。

- **地名も座標も送らない**（URL に載るが、こちらのサーバーへは送らない）
- Cookie も識別子も使わない。Referer も止めてある
- ⚠ **IP について**: このアプリは **IP アドレスを保存せず、利用者の識別にも使わない**。
  ただし通信そのものは Cloudflare を通るので、**基盤側が接続元 IP を扱うことは避けられない**
- 計測（`/t`）に送るのは、**列挙した固定文字列だけ**

これが破れている経路を見つけたら、脆弱性として扱ってほしい。

## 対象外

- **国土地理院・Wikidata・OpenStreetMap 側の問題**
  （こちらからは直せない。ただし、こちらの使い方が悪くて負荷をかけているなら報告してほしい）
- ⚠ **`wrangler.jsonc` の `database_id`**
  これは識別子であって資格情報ではない。操作には Cloudflare の認証が別に要る
- ⚠ 走査ツールの出力そのものを**一律に対象外にはしない**。
  ただし、**再現できる影響を示せない場合は、そこで終了することがある**

## 直したあと

影響と対策は、安全に書ける範囲で公開する（Issue または Security Advisory）。
⚠ **再現手順は、直ってから出す。**
