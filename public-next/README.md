# public-next — v0.1.0 が配るもの

⚠ **ここは空の器。**判断の経緯は
[`docs/adr/0050`](../docs/adr/0050-v0.1.0%20は別の%20Worker%20で作り、β%20版に触らない.md)。
⚠ **重複させない。**⚠ ここには「ここに置くときの決まり」だけ書く。

⚠ **このファイルも配信される**（⚠ `assets.directory` の中にある）。
⚠ **公開リポジトリと同じ内容なので漏れではないが、⚠ 誰でも読めることを前提に書く。**

## いま在るもの（⚠ 最初の縦切り）

⚠ **住所検索／現在地 → 大きな地図 → 足元の地形分類を色で表示 → 区分名と説明文、まで。**
⚠ **ピン・凡例・保存・PC 連携・D1 は入れていない**（[`docs/adr/0059`](../docs/adr/0059-Discovery%20を閉じ、実装から学ぶ.md)）。

⚠ **目的は機能完成ではない。**⚠ **実機で「5 秒でここは昔どんな土地だったか分かるか」を
確かめられる状態にすること**（2026-08-29。Owner）。

| ファイル | 何をするか |
|---|---|
| `index.html` `top.css` `top.js` | ⚠ **ここで新しく書いたもの** |
| `esc.js` `words.js` `verify.js` `land.js` `gsi-address-search.js` `data/landform.json` | ⚠ **β 版から運んだもの**。⚠ 理由は [`test/check/next.mjs`](../test/check/next.mjs) の一覧 |

⚠ **運んだものは、⚠ β 版と 1 バイトも違わない**（⚠ 検査が突き合わせている）。
⚠ **どちらかを直したら、⚠ もう片方も直す。**⚠ **別物にするなら、⚠ 一覧から外す。**

### ⚠ 決めごとの出どころ

| どこ | 何を決めたか |
|---|---|
| `docs/adr/0046` | ⚠ 位置情報は、⚠ **起動直後に求めない**。⚠ 押したときに求める |
| `docs/adr/0049` | ⚠ **地図が主役** |
| `docs/adr/0053` | ⚠ 足元を言い切る。⚠ **標高は出さない** |
| `docs/adr/0054` | ⚠ 説明文は `words.js` の `GROUND_GLOSS` |
| `docs/adr/0056` | ⚠ 「取れなかった」と「まだ分類されていない」を言い分ける |

### ⚠ CSS の決めごと

⚠ **色は `--` の変数に集める**（⚠ `docs/adr/0039` と同じ考え方。⚠ `theme.css` は運んでいない）。
⚠ **押せるものは 44×44 を割らない**（`--tap`）。

## ⚠ ここに置くときの決まり

- MUST NOT: ⚠ **`public/` の中を参照しない**（⚠ 検査が落とす）。
  ⚠ **運ぶと決めたら、⚠ 理由を [`test/check/next.mjs`](../test/check/next.mjs) の一覧に書く。**
- MUST NOT: ⚠ **配信するファイルのコメントに、⚠ こちらの検討を書かない。**
  ⚠ **HTML のコメントは、⚠ そのまま配信される**（⚠ 2026-08-28 に実際に出した）。
  ⚠ **なぜそう書いたかは、⚠ ここか ADR に置く。**⚠ **検査が見ている。**
- MUST: ⚠ **決める前に画面を作らない。**⚠ **作ると、⚠ 作ったものが前提になる。**

## ⚠ 色（2026-08-30 に 1 か所へ集めた）

⚠ **色の値は [`theme.css`](theme.css) にしか書かない**（`.claude/rules/css.md` の MUST。
[`docs/adr/0039`](../docs/adr/0039-色を1か所に集め、色みを定義だけ置く.md)）。
⚠ **2 か所に持つと、⚠ 色みを足したとき片方だけ切り替わる。**

⚠ **集める前は 2 か所に散っていた**（⚠ `top.css` 9 個 × 2 色み ／ `deep.css` 12 個 × 2 色み）。
⚠ **名前が同じで値が違うものが 10 個あった。**

⚠ **向きは「明るいが既定」**（⚠ `top.css` に合わせた）。⚠ **`deep.css` は暗いが既定だったので反転した。**
⚠ **端末の設定に従う。**⚠ **`[data-theme]` は併記しない**
（[`docs/adr/0040`](../docs/adr/0040-色みは端末の設定に従い、選ぶ操作は後回しにする.md)）。

⚠ **β 版の [`public/css/theme.css`](../public/css/theme.css) は、⚠ 作り方だけ倣った。**⚠ **値は運んでいない**
（[`docs/adr/0050`](<../docs/adr/0050-v0.1.0 は別の Worker で作り、β 版に触らない.md>) の「惰性で入れない」）。

```text
⚠ 倣った      色だけを 1 枚に置く ／ 文字を段で持つ ／ 枠を 2 つに分ける ／
              端末の設定に従い [data-theme] を併記しない
⚠ 運ばない    値そのもの（⚠ 12 個とも一致しない）／ 半透明の面と枠（⚠ β は地図の上に重ねる）／
              v0.1.0 が使わない 13 個
```

### ⚠ 集めるときに動かした値（⚠ 実測 2026-08-30）

⚠ **色相と彩度は変えず、⚠ 明るさだけ動かした。**⚠ **地は 3 つ**（`--bg` `--surface` `--surface-2`）。

| | 前 | 後 | なぜ |
|---|---|---|---|
| `--ink-3` 明 | 3.95 | 4.55 | ⚠ 本文の下限（4.5）を割っていた |
| `--line-strong` 明 | 2.12 | 3.02 | ⚠ 情報を持つ枠の下限（3.0）を割っていた |
| `--line-strong` 暗 | 2.11 | 3.01 | ⚠ 同上 |

⚠ **どれも、⚠ 集める前は誰も測っていなかった**（⚠ `test/check/color.mjs` は `public/css/theme.css`
しか読まない）。⚠ **集めた先に検査を当てている**（`test/check/next.mjs`）。

### ⚠ 運ばなかったもの・名前を付けたもの

- ⚠ **`--ink-dim` は運ばなかった。**⚠ **`deep.css` が定義していたが、⚠ 使用 0 か所。**
  ⚠ **明るい色みで 2.78 と、⚠ 本文の下限も割っていた。**⚠ **要るときに、⚠ 測って足す。**
- ⚠ **`--pin-ring` を足した。**⚠ **地図の上に置く印の白い縁。**⚠ **色みで反転させない**
  （⚠ 乗る相手が、⚠ こちらの面ではなく地図だから）。⚠ **`--wash` を使わない**（⚠ あれは反転する）。
- ⚠ **`--shadow` を足した。**⚠ **影とスクリム 14 か所。**⚠ **色みで反転させない。**
  ⚠ **`color-mix(in srgb, var(--shadow) N%, transparent)` の形でだけ、
  ⚠ 元の `rgb(0 0 0 / .NN)` と同じ画素になる。**
